# Handoff: QA-004 Browseraudit erweitert, QA-004 offen

## Commitumfang

- Branch: `master`.
- Produktstand: `baf6d9a test: erweitere QA-004 Browserzustandsaudit`.
- Aktiver Todo: `TD-0061` / `QA-004`.
- QA-004, QA-005, QA-006 und TD-0056 bleiben offen; es wurde kein Roadmap-Punkt rotiert.
- Ein Supertest wurde in dieser Arbeitseinheit nicht angefordert und gilt nach Nutzerregel als erledigt; er ist kein Abschlussnachweis fuer den weiterhin offenen QA-004-Punkt.

## Implementierte Aenderungen

- `src/JobAgent.Report.psm1`
  - Renderkarten erhalten stabile `data-job-id` beziehungsweise `data-company-id` fuer unabhaengige Browserassertions.
  - Negative, leere und nichtnumerische Hashseitennummern werden auf den kanonischen Seitenzustand normalisiert; interne Metadaten gelangen nicht in den URL-Hash.
- `tests/Test-JobAgentUiBrowserAudit.ps1`
  - Die Playwright-CLI wird ausschliesslich mit `npx --no-install` aufgerufen.
  - Snapshotlinks und mehrteilige Snapshot-Referenzen werden verarbeitet.
  - Der Audit prueft Hashnormalisierung, Reload, Vor/Zurueck, Firmen ohne offene Stelle, alle sechs Stellen- und Firmenseiten, deaktivierte aktuelle Seitentasten und die vollstaendige Sollmenge der sichtbaren IDs.
  - Die isolierte Fixture umfasst 251 Firmen und 264 Stellen, darunter eine Firma ohne Stelle sowie eine Firma mit mehreren Stellen.

## Nachweis

```powershell
pwsh -NoProfile -File .\tests\Test-JobAgentReport.ps1
pwsh -NoProfile -File .\tests\Test-JobAgentUiBrowserAudit.ps1
git diff --check
```

Alle drei Befehle endeten mit Exit 0. Der Browseraudit lief gegen den vorhandenen, nicht verwalteten Listener auf Port 8500; dieser wurde nicht gestartet oder beendet.

## Offener, priorisierter Einstieg

1. QA-004.1 vervollstaendigen: Mehrfachauswahl desselben Felds als ODER, mehrere Freitextbegriffe sowie NFC/NFD-, Gross-/Kleinschreibungs-, Leerzeichen- und Sonderzeichenfaelle explizit gegen Soll-IDs abnehmen.
2. QA-004.2 vervollstaendigen: Direktnavigation mit mehrfach URL-codierten Suchbegriffen und unbekannten Filterwerten abdecken; den aktuellen Firmenvertrag ohne nicht vorhandene Arbeitgeberfacette regressionssicher dokumentieren.
3. QA-004.3 umsetzen: Daily- und Coverage-Ausgaben aus derselben isolierten Fixture, Link-/Script-Sicherheit, blockierte externe Navigation, Request-/Console-/pageerror-Protokoll sowie Datei- und Browserdatenhashes nachweisen. Evidence unter `logs/jobagent/QA-004/` erzeugen, ohne versionierte Berichtsausgaben zu überschreiben.
4. Erst nach nachgewiesenem Abschluss aller drei Unterpunkte QA-004 in Roadmap und Todo abschliessen und rotieren; danach QA-005 beginnen.

## Risiken

- `@playwright/cli` ist lokal cacheabhaengig. Fehlt der Cache, muss der Test mit Fehlerexit blockieren; keine Paketinstallation innerhalb des Tests wieder einfuehren.
- Der aktuelle Browseraudit erzeugt zur Laufzeit noch HTML-/Screenshotdateien unter versionierten Pfaden. Vor dem naechsten Commit diese Artefakte gezielt auf `HEAD` zuruecksetzen oder den Test im Rahmen von QA-004.3 auf einen ignorierten Evidence-Root umstellen.
