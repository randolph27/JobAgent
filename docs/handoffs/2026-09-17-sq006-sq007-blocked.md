# Übergabe – SQ-006 abgeschlossen, SQ-007 blockiert

Stand: 2026-09-17

## Abgeschlossener Arbeitsschnitt

SQ-006 ist nach `Roadmap_archive.md` rotiert. Der Analysevertrag bleibt technisch ehrlich und begrenzt den Umfang auf `external-powershell-issues-only`:

- SonarQube Server: `9.9.8.100196`.
- Vorgesehener Scanner: SonarScanner CLI `4.8.1.3023` mit Java `11`.
- Vorgesehener externer Analyzer: PSScriptAnalyzer `1.25.0`.
- Ein Community-SonarQube-Plugin wurde nicht installiert.
- Native PowerShell-Regeln, Coverage, Duplikatmetriken, Quality-Profile-Regeln und Quality-Gate-Aussagen sind ausgeschlossen.
- `.ci/tools/sonar` ist die einzige erlaubte, gitignorierte Artefaktwurzel. PATH-, Benutzerprofil- und globale Modulquellen sind verboten.

Die Konfiguration bleibt absichtlich `sonar.mode:not-supported` und `sonar.toolchain.status:not-installed`. Der vorhandene Java-PATH-Runner meldet Version `26`; er ist nicht für diese Lieferkette freigegeben. Es existieren noch kein Scanner, kein PSScriptAnalyzer-Artefakt, kein Generic-Issue-Bericht, kein SonarQube-Projekt-Key und keine Analyse-ID.

## Versionierte Artefakte

- `docs/reviews/SQ-006-analysis-supply-chain.md`: offizielle Quellen, Analysegrenzen und Ablehnung eines Community-Plugins.
- `docs/reviews/SQ-006-toolchain.json`: erwartete Versionen, Quellen, Lizenzen und Projektpfade; SHA-256 ist bewusst `null`, solange kein Artefakt beschafft wurde.
- `tests/Test-SonarToolchain.ps1`: prüft Root-Containment und weist fehlende, fremde sowie hashabweichende Artefakte fail-closed ab.
- `.ci/ci.config.json`: zentraler `not-installed`-Lieferkettenvertrag; der dazugehörige Immutable-Pin und Snapshot wurden aktualisiert.

## Nachgewiesene Validierung

- `pwsh -NoProfile -File .\tests\Test-SonarToolchain.ps1` – Exit `0`; sechs Lieferkettenfälle.
- `pwsh -NoProfile -File .\tests\Test-SonarAuth.ps1` – Exit `0`.
- `pwsh -NoProfile -File .\tests\Test-JobAgentCiContracts.ps1` – Exit `0`.
- `./ci.cmd sonar-auth` – Exit `0`; Server erreichbar und Token sekretfrei gültig.
- `./ci.cmd self-check` – Exit `0`.
- `./ci.cmd supertest` – Exit `0`; `29/29` bestanden, `0` fehlgeschlagen, `0` blockiert, `0` nicht ausgeführt, Laufzeit `703,16 s`.

## Aktiver Blocker: TD-0068 / SQ-007

SQ-007 ist aktiv und `blocked`. Für die nächsten Implementierungsschritte fehlt die explizite Freigabe für folgende externe Zustandsänderungen:

1. Die drei vorgesehenen Artefakte herunterladen und ausschließlich unter `.ci/tools/sonar` ablegen.
2. Die tatsächlichen SHA-256-Werte erfassen, in `.ci/ci.config.json` und `docs/reviews/SQ-006-toolchain.json` eintragen und den Lieferkettenstatus auf einen verifizierten Zustand umstellen.
3. Erst danach, separat, eine SonarQube-Projektanlage durchführen und External Issues hochladen.

Ohne Schritt 1 und 2 darf kein Scannerlauf gestartet werden. Ohne die getrennte Projektfreigabe darf keine SonarQube-Projektanlage oder Analyse gestartet werden. Die vorhandene Token-Datei bleibt außerhalb des Repositories; weder ihr Inhalt noch Header, Query-Parameter oder Tokenlängen dürfen ausgegeben oder versioniert werden.

## Fortsetzung nach Freigabe

1. Beschaffte Dateien gegen die eingetragenen SHA-256-Werte prüfen; fehlende oder abweichende Dateien müssen den bestehenden Toolchain-Test weiterhin fail-closed beenden.
2. Für SQ-007 zuerst nur Fixture-basierte Helper und `tests/Test-SonarExternalIssues.ps1` implementieren: Include-/Exclude-Satz, PSScriptAnalyzer-Normalisierung und SonarQube-9.9-Generic-Issue-JSON mit projektrelativen Pfaden.
3. Negative Tests für leeren Bericht, unbekannte Regel, Pfad außerhalb des Roots, ungültige Zeilenbereiche, abgelaufene Task-Antwort und Scanner-Exit ungleich `0` ausführen.
4. Erst mit separater Projektfreigabe einen einzelnen Serverimport durchführen, Compute-Engine-Task bis zum terminalen Status abfragen und nur sekretfreie Metadaten dokumentieren.
5. SQ-008 erst nach einem erfolgreichen und einem erwarteten negativen SQ-007-Import beginnen.

## Unveränderbare Grenzen

- Keine globale Scanner-, Java- oder PowerShell-Modulinstallation.
- Keine Analyse von `data/`, `logs/`, `.git/`, Caches oder Testausgaben.
- Keine Behauptung einer nativen PowerShell-Analyse, Coverage oder fachlichen Quality-Gate-Freigabe.
- Keine automatische Änderung von Quality Gates oder Quality Profiles.
- Keine Commits oder Pushes aus dem zukünftigen `sonar`-Command.
