# Übergabe nach SQ-008 und SQ-009

Stand: 2026-09-17 15:23 CEST

## Abgeschlossener Schnitt

SQ-008 und SQ-009 sind vollständig abgeschlossen, aus `Roadmap.md` nach `Roadmap_archive.md` rotiert und als `TD-0069` sowie `TD-0070` im Todo-Index abgeschlossen. `Roadmap.md` und `todo.current.md` enthalten keine aktiven Aufgaben.

Der kanonische Command `./ci.cmd sonar` führt ausschließlich den begrenzten External-Issue-Lifecycle aus. Der feste Analyseumfang ist `external-powershell-issues-only`: PSScriptAnalyzer-Befunde aus `.ci/bin` und `tools`. Er ist keine native PowerShell-Analyse und enthält keine Aussage zu Coverage, Duplikaten, Quality Profiles oder Quality Gates.

## Letzter erfolgreicher Servernachweis

- Command: `./ci.cmd sonar`
- Exit: `0`
- Evidence: `logs/verify/sq-009-20260917-152135.json`
- Projekt: `jobagent-external-powershell`
- Commitreferenz: `027fe60185a190ad5eb56159e3140377d186d329`
- Befunde: `323`
- Compute-Engine-Endpointklasse: `sonar-api-ce-task`
- HTTP-Status: `200`
- Task: `AaCvh97JOhdKqVTDjfxk`
- Analyse: `AaCvh-LnotDYp2WltY0K`
- Terminalstatus: `SUCCESS`

`logs/verify/verify.digest.json` und `handoff.latest.*` referenzieren dieselbe Analyse-ID. Die Evidence enthält keine Token, Header, Queryparameter, Tokenlängen, vollständigen Responses oder Scanner-Debugausgaben.

## Implementierung und Fehlervertrag

In `.ci/bin/modules/ci-commands-main.ps1` trennen `Get-SonarComputeEngineReadFailureClass` und `Invoke-SonarComputeEngineTaskRead` die Compute-Engine-API von der allgemeinen Sonar-API. Die Fehlerevidence bleibt fail-closed und sekretfrei für 401, 403, 404, 5xx, HTTP-Timeout, Transporttimeout, Transportfehler, ungültige Task-ID, fehlendes oder ungültiges Taskobjekt, `FAILED`, `CANCELED`, ausstehende Tasks bis Timeout sowie ungültige Analyse-ID.

Der frühere Fehler `sonar_external_compute_engine_lookup_failed` war keine Serverursache: `Get-SonarCommitReference` verlor durch eine Pipeline den Git-Exitcode nach einem erfolgreichen Taskread. Der Commit-Read speichert den Prozess-Exitcode jetzt vor weiterer Pipelineverarbeitung.

## Prüfstand

Am 2026-09-17 mit Exit `0` ausgeführt:

- `pwsh -NoProfile -File .\tests\Test-SonarCiLifecycle.ps1`
- `pwsh -NoProfile -File .\tests\Test-SonarExternalIssues.ps1`
- `pwsh -NoProfile -File .\tests\Test-SonarAuth.ps1`
- `pwsh -NoProfile -File .\tests\Test-SonarToolchain.ps1`
- `pwsh -NoProfile -File .\tests\Test-JobAgentCiContracts.ps1`
- `./ci.cmd sonar`
- `git diff --check` und JSON-Validierung der Todo-, Handoff- und Sonar-Evidence

Ein Supertest wurde nicht ausgeführt. Er gilt gemäß Nutzerregel als erledigt und ist kein Ersatz für die gezielten Sonar-Funktionstests.

## Weiterarbeit

Es gibt keinen offenen Roadmap- oder Todo-Punkt. Ein neuer Auftrag benötigt einen neuen priorisierten Roadmap-Punkt. Vor einem neuen Sonar-Feature die Grenzen aus `docs/reviews/SQ-008-ci-lifecycle-acceptance.md` und `docs/reviews/SQ-009-compute-engine-read-acceptance.md` beibehalten: keine Projekt-, Token-, Quality-Gate-, Quality-Profile-, Server- oder Toolchain-Pin-Mutation ohne expliziten Auftrag.
