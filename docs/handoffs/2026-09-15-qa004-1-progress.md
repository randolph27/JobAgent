# Handoff: QA-004.1 gestartet, Browsernachweis offen

## Commit-Stand

- Branch: `master`.
- Aktiver Todo: `TD-0061` / `QA-004`.
- QA-004, QA-005 und QA-006 bleiben offen; es wurde kein Roadmap-Punkt rotiert.
- Der vom Nutzer nicht angeforderte Supertest ist fuer diesen Uebergang nicht auszufuehren und blockiert keinen Statuswechsel.

## Gelieferte Aenderungen

- `src/JobAgent.Report.psm1`: Eine Stelle mit unbekanntem Zielgebiet erhaelt jetzt die Facette `UNKNOWN`. Damit ist die bereits angebotene Gebietsauswahl `Unbekannt` funktional erreichbar.
- `tests/fixtures/jobagent/qa-004-ui-contract.json`: Vertrag fuer den festen Bezugszeitpunkt, Grenzmengen `0/1/49/50/51/250/251`, alle auswählbaren Facetten und Alterswerte.
- `tests/Test-JobAgentUiBrowserAudit.ps1`:
  - Fixture auf 251 Firmen und 264 Stellen erweitert;
  - enthält Freising-, München-20-km-, Remote-, UNKNOWN- sowie alle Arbeitsmodell- und Anstellungsartwerte;
  - prüft 7-, 30-, über-30-Tage- und UNKNOWN-Alter;
  - enthält einen fokussierten `-FixtureOnly`-Modus ohne Browser;
  - enthält echte Browseraktionen für jede vorhandene Facettenoption, jeweils mit Reset-Assertion.

## Verifikation

Erfolgreich:

```powershell
pwsh -NoProfile -File .\tests\Test-JobAgentUiBrowserAudit.ps1 -FixtureOnly
pwsh -NoProfile -File .\tests\Test-JobAgentReport.ps1
```

Der Fixturetest meldet 251 Firmen, 264 Stellen sowie alle sieben Grenzmengen. Der Reporttest lief mit Exit 0.

Nicht erfolgreich abgeschlossen:

```powershell
pwsh -NoProfile -File .\tests\Test-JobAgentUiBrowserAudit.ps1
```

Die Browserlane ruft pro Interaktion `npx --yes --package @playwright/cli` auf. Im eingeschränkten Lauf scheiterte dies reproduzierbar beim Zugriff auf `C:\Users\ralph\AppData\Local\npm-cache` mit `EPERM`. Der vollständige Browsernachweis ist deshalb nicht als bestanden zu werten.

## Nächster konkreter Schritt

1. Eine lokal installierte oder vollständig cachebare Playwright-CLI bereitstellen; der Test darf keine Paketinstallation während der Ausführung benötigen.
2. `./ci.cmd devserver-status` ausführen und ausschließlich bei fehlender Instanz `./ci.cmd devserver-start` verwenden.
3. Den vollständigen Browseraudit erneut starten. Erst nach dessen Erfolg QA-004.1 in der Roadmap abhaken.
4. Danach QA-004.2 umsetzen: Tabs, URL-Hash-Normalisierung, Reload, Vor/Zurück, alle Seiten und Firmenansicht mit exakten ID-Assertions.
5. QA-004.3 folgt erst danach. QA-005 und QA-006 bleiben abhängig von vollständigem QA-004.

## Aufräumstatus

- Die vom fehlgeschlagenen Browserlauf angelegte lokale `.playwright-cli`-Sitzungsablage wurde entfernt.
- Generiertes HTML aus dem abgebrochenen Lauf wurde auf `HEAD` zurückgesetzt.
