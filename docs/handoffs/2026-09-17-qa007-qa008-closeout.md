# Übergabe – QA-007 und QA-008 abgeschlossen

Stand: 2026-09-17

## Arbeitsstand

Es gibt keine aktiven Roadmap- oder Todo-Punkte. QA-007 und QA-008 sind nach `Roadmap_archive.md` rotiert; die zugehörigen Abnahmen stehen unter `docs/reviews/QA-007-acceptance.md` und `docs/reviews/QA-008-acceptance.md`.

Die einzige fachliche Quelländerung dieses Abschlusses liegt ausschließlich in `tests/Test-Ci005Invariants.ps1`. Sie ersetzt das frühere mutierende Fixture-Szenario durch einen direkten, nicht mutierenden CI-005-Vertragstest. Es wurden keine App-, Produkt-, Daten-, SonarQube- oder Zugangsdaten-Dateien geändert.

## Kaspersky-Kontext

Der Nutzer zeigte ein Kaspersky-System-Watcher-Fenster für `tests/Test-JobAgentCiContracts.ps1` mit der Erkennung `PDM:Trojan.Win32.Generic`. Es wurde keine Ausnahme erstellt, keine Desinfektion ausgelöst und keine Schutzrichtlinie geändert. Die Ursache wurde auf die vorherige Testausführung eingegrenzt: pro Negativfall temporäres Repository kopieren, Fixture modifizieren, verschachtelt `ci.cmd self-check` starten und das Fixture rekursiv löschen. Diese Abläufe sind aus `Test-Ci005Invariants.ps1` entfernt.

Falls später erneut eine Security-Meldung erscheint, zuerst den genauen Prozesspfad und den Quarantänestatus außerhalb des Repositorys prüfen. Keine Ausnahme oder Remediation ohne ausdrückliche Nutzerentscheidung durchführen. Änderungen weiterhin auf Testscripts begrenzen, sofern keine unmittelbare Kausalität zu einer anderen Datei nachgewiesen ist.

## Nachweise

Folgende funktionalen Befehle waren auf diesem Arbeitsstand erfolgreich:

```powershell
pwsh -NoProfile -File .\tests\Test-Ci005Invariants.ps1
pwsh -NoProfile -File .\tests\Test-JobAgentCiContracts.ps1
pwsh -NoProfile -File .\tests\Test-JobAgentTestMatrix.ps1
pwsh -NoProfile -File .\tests\Test-JobAgentSupertestContract.ps1
.\ci.cmd self-check
.\ci.cmd supertest
```

Der vollständige Supertest endete mit Exit 0 nach 692,59 Sekunden: `planned_test_count=29`, `passed=29`, `failed=0`, `blocked=0`, `not_run=0`.

## Inventar

`docs/reviews/QA-001-function-inventory.json` wurde durch `Test-JobAgentTestMatrix.ps1 -WriteInventory` neu generiert und enthält 577 Einträge. Vor der Regenerierung unterschieden sich bei gleicher Symbol- und Katalogmenge fünf Quellhashes in `tools/Invoke-JobAgentDailyRun.ps1`; keine Hashwerte wurden manuell gesetzt.

## Nächster Arbeitsanker

Kein offener Arbeitsauftrag. Der nächste Agent soll zuerst `README.md`, `Roadmap.md`, `todo.current.md`, `todo.state.json` und `handoff.latest.md` lesen. Neue Anforderungen als neue Roadmap-/Todo-Punkte erfassen; ohne neue Anforderung keine Produktlogik ändern. Bei reinem Testinfrastrukturbedarf ausschließlich die betroffenen Testscripts und zugehörige Evidenz ändern.
