# Handoff – CI-005 abgeschlossen, SQ-001 blockiert

Stand: 2026-09-16

## Ausgangspunkt für den nächsten Chat

- Arbeitszweig vor Commit: `master`, HEAD `aee7a9553e81`, Upstream `origin/master`.
- Der aktuelle Arbeitsstand wird mit diesem Handoff, den aktualisierten Todo-Dateien und der Roadmap committed und gepusht.
- Beim Einstieg zuerst `git status --short`, `Get-Content -Raw handoff.latest.md`, `Get-Content -Raw todo.current.md` und `Get-Content -Raw Roadmap.md` ausführen.

## Abgeschlossen: CI-005

- Der Hashkonflikt von `manual/PROGRAM.md` ist als überholter Snapshot identifiziert. Der aktuelle Programmvertrag hat SHA-256 `62E616E167CB6F6F707726F84D96B1987066B170BE38A2EE4486927DC0F9EDF3` und keinen Git-Diff gegen HEAD.
- Nur `.ci/pins/immutable.hashes.json` und `.ci/pins/immutable.snapshot/manual/PROGRAM.md` wurden gezielt synchronisiert; die produktive Programmdokumentation blieb unverändert.
- `tests/Test-Ci005Invariants.ps1` führt vier isolierte Negativfälle aus: geänderte Immutable-Datei sowie fehlende Werte für Branch, HEAD und Worktree im Handoff-Markdown.
- `tests/Test-JobAgentCiContracts.ps1` führt den CI-005-Test mit aus.
- `Roadmap.md` enthält nur SQ-001. CI-005 ist vollständig nach `Roadmap_archive.md` rotiert. Der nicht angefragte Supertest ist gemäß Nutzerregel als erledigt markiert.
- TD-0056 wurde als erledigter Drift-Tracker in die Todo-Historie überführt.

## Bestandene Nachweise

- `pwsh -NoProfile -File .\tests\Test-JobAgentCiContracts.ps1` – Exit 0, einschließlich CI-005-Isolationstest.
- `.\ci.cmd self-check` – Exit 0; jüngste Evidence: `logs/terminal/self-check-20260916-172842.log`.
- `.\ci.cmd route-check` – Exit 0; Evidence: `logs/terminal/route-check-20260916-172439.log`.
- `.\ci.cmd observer-baseline` – Exit 0.
- `.\ci.cmd drift-check` – Exit 0; `logs/observer/drift-latest.json` enthält `immutables_ok: true`, `route_ok: true` und `drift: false`.
- `git diff --check` – Exit 0 vor der Übergabe.

## Aktiver Blocker: SQ-001 / TD-0064

- Todo: `TD-0064` ist aktiv und `blocked`; `handoff.latest.md` enthält die nächste konkrete Aktion.
- SonarQube auf `http://127.0.0.1:9000` ist erreichbar (`UP`).
- Der Token aus `D:\_Scripte\_Sonar\token.txt` wurde nicht ausgegeben. `GET /api/authentication/validate` ergab `valid:false`; authentifiziertes `GET /api/projects/search?ps=1` ergab HTTP 401.
- `.ci/ci.config.json` bleibt auf `sonar.mode: not-supported`; kein Scanner, keine Projektanlage und keine Analyse wurden gestartet.
- Der vollständige sekretfreie Nachweis steht in `docs/reviews/SQ-001-acceptance.md`.

## Nächster fachlicher Ablauf

1. Einen neuen lokalen SonarQube-Token mit Browse- und Execute-Analysis-Berechtigung für den vorgesehenen Projekt-Key bereitstellen.
2. Den API-Read aus `docs/reviews/SQ-001-acceptance.md` wiederholen. Erst `valid:true` und HTTP 200 erlauben die Fortsetzung.
3. Für PowerShell-, JSON- und statische HTML-Dateien die unterstützte Scanner-/Analyzer-Kombination anhand offizieller SonarQube-Dokumentation belegen.
4. Erst dann minimalen projektlokalen Analysemodus, Scannerkonfiguration und Erfolg-/Fehlerfunktionstests ergänzen. Keine Token in Dateien, Logs, Diff oder Prozessargumente schreiben.
5. SQ-001 erst nach Scannerlauf, neuer Analyse-ID und grünem Quality Gate aus der Roadmap rotieren. Ein nicht angefragter Supertest gilt als erledigt, ersetzt aber keine Sonar-Akzeptanzkriterien.

## Wichtige Dateien

- `docs/reviews/CI-005-acceptance.md`: Hash-Herkunft, Drift- und Testnachweis.
- `docs/reviews/SQ-001-acceptance.md`: aktueller Sonar-Blocker und sekretfreier Reproduktionstest.
- `tests/Test-Ci005Invariants.ps1`: neue CI-005-Funktionsprüfung.
- `handoff.latest.md`, `todo.current.md`, `todo.state.json`: maschinenlesbarer und menschlicher Wiederanlaufzustand.
