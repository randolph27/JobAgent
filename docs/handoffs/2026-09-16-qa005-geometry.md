# Handoff: QA-005 Geometriegrundlage

Stand: 2026-09-16T00:30:00+02:00

## Aktiver Auftrag

- Aktives Todo: `TD-0062` / Roadmap `QA-005`.
- Der Roadmap-Punkt ist nicht abgeschlossen und bleibt in `Roadmap.md` aktiv.
- `QA-005.1` ist begonnen, aber noch nicht vollstaendig. Kein Roadmap-Rotationskandidat.
- `QA-006` und `TD-0056` bleiben unveraendert offen.

## Umgesetzter Stand

- `tests/fixtures/jobagent/QA-005-visual-contract.json` definiert die verbindlichen Viewports `390`, `800`, `1366` und `1920`, maximal einen CSS-px Root-Overflow sowie die Mindest-Controlgroesse `44` CSS-px.
- `tests/Test-JobAgentUiBrowserAudit.ps1` misst fuer Initialansicht, komplexen Filter, Nulltreffer, Firma ohne offene Stelle, letzte Stellenseite und langen Titel `documentElement.scrollWidth` sowie sichtbare Button-/Input-/Select-Boundingboxes. Die Messwerte werden unter `geometry` in der Browser-Evidence gespeichert.
- Die Fixture enthaelt einen langen ungetrennten Titel. Der Audit stellt dessen Erreichbarkeit im Browser fest.
- `src/JobAgent.Report.psm1` setzt fuer `input`, `select` und `button` jetzt `min-width: 44px` und `min-height: 44px`. Das behebt den reproduzierten Befund: Pagination-Buttons waren bei 390 px schmaler als 44 CSS-px.

## Verifikation

- `pwsh -NoProfile -File .\tests\Test-JobAgentHtmlAudit.ps1` -> Exit 0.
- `pwsh -NoProfile -File .\tests\Test-JobAgentUiBrowserAudit.ps1` -> Exit 0, isolierte Fixture und lokaler CI-Devserver.
- Aktuelle Browser-Evidence: `logs/jobagent/QA-004/qa004-0ccea5ec1f124b6e8edb3a59a99170f9/browser-cases.json`.
- Der Browserlauf benoetigt Zugriff auf den vorhandenen npm-/Playwright-Cache ausserhalb der Sandbox. Im Sandbox-Modus trat `EPERM` beim npm-Cache auf; der erfolgreiche Lauf erfolgte ausserhalb der Sandbox.
- Kein Supertest: Der Nutzer hat ihn fuer diesen Arbeitsschnitt nicht angefragt; nach Nutzerregel ist kein Supertest als Restpflicht zu behandeln.

## Naechster konkreter Arbeitsschritt

QA-005.1 im selben Browseraudit vervollstaendigen:

1. Geometrische Overlap-Pruefung zwischen verschiedenen sichtbaren bedienbaren Controls ergaenzen, mit begruendeten Ausnahmen nur fuer Inline-Links und Tabellen-Scrollcontainer.
2. 200-%-Zoom auf Desktopbreite, deaktivierte Animationen, abgeschlossenen Fontladezustand sowie explizite Vollinhalts-/Clipping-Messung ergaenzen.
3. Danach ausschliesslich den betroffenen Browseraudit erneut ausfuehren. Erst nach Abschluss von QA-005.1 folgen QA-005.2 (Tastatur, Semantik, Kontrast) und QA-005.3 (Screenshots, Sichtung, Negativfixtures).

## Grenzen

- Keine Android-/ADB-Lane: fuer diesen Web-Bericht `not-applicable`.
- Keine produktiven Daten mutieren; Browseraudit arbeitet mit isolierter Fixture.
- Keine Referenzbilder automatisch freigeben oder Baselines ueberschreiben.
