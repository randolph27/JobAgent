# Handoff: QA-004 Browseraudit erweitert, Abschlussnachweis offen

## Zustand

- Aktiver Todo: `TD-0061` / `QA-004`; QA-004 bis QA-006 und TD-0056 bleiben offen.
- Kein Roadmap-Punkt ist vollständig genug für eine Rotation. Supertest wurde nicht angefragt.

## Aenderungen

- `src/JobAgent.Report.psm1`: Mehrfach URL-codierter Freitext wird begrenzt dekodiert und kanonisch in den Hash zurückgeschrieben.
- `tests/Test-JobAgentUiBrowserAudit.ps1`: isolierte Evidenz in `logs/jobagent/QA-004/<run-id>/`; neue Assertions für OR/AND, NFC/NFD, mehrfache URL-Codierung, Sonderzeichen, Firmenfreitext ohne Arbeitgeberfacette, Script-Inertheit, sichere und abgefangene Linkziele, Hashes und Browserfehler.

## Verifikation

- `pwsh -NoProfile -File .\tests\Test-JobAgentUiBrowserAudit.ps1 -FixtureOnly` -> `status: ok`.
- Der vollständige Browseraudit korrigierte bereits Listener-Quoting, JSON-Eval, Hashnormalisierung und die Linkansicht. Letzter Fehler: externe Request-Assertion. Sie enthält nun die vollständige Request-Ausgabe, wurde danach noch nicht erneut ausgeführt.

## Naechster Schritt

1. Vollständigen Browseraudit ausführen und bei Fehler die Request-Ausgabe auswerten.
2. Nur echte externe Requestzeilen als Fehler bewerten; keine externe Navigation zulassen.
3. Nach grünem Audit QA-004.3-Evidence abschließen; erst dann QA-004 rotieren. Danach QA-005.

## Grenzen

- Fixtureaufbau benötigt etwa eine Minute CPU. Browseraudit unsichtbar mit lokalen ignorierten Logs starten.
- `npx --no-install`; keine Paketinstallation. Port 8500 nicht ohne CI-Identitätsbeleg beenden.
