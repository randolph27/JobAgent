# Handoff: QA-004 abgeschlossen, QA-005 aktiv

## Aktueller Zustand

- Aktiver Todo: `TD-0062` / `QA-005`.
- `QA-004` ist nach `Roadmap_archive.md` rotiert; `TD-0061` ist abgeschlossen.
- Branch: `master`; QA-004 ist als Commit `87070fc` nach `origin/master` gepusht.
- Der reguläre Supertest wurde nicht ausgeführt. Nach expliziter Nutzerregel gilt er als erledigt und ist für QA-004 im Archiv entsprechend vermerkt.

## QA-004-Nachweis

- `pwsh -NoProfile -File .\tests\Test-JobAgentReport.ps1` -> Exit 0.
- `pwsh -NoProfile -File .\tests\Test-JobAgentCoverage.ps1` -> Exit 0.
- `pwsh -NoProfile -File .\tests\Test-JobAgentUiBrowserAudit.ps1` -> Exit 0.
- Isolierte Evidence: `logs/jobagent/QA-004/qa004-f8b0fc7bbe1043e5a287b594afd1c8bf/browser-cases.json`.
- Die serialisierte Fixture und der erzeugte Daily-/Coverage-Bericht haben vor und nach den UI-Interaktionen gleiche SHA-256-Hashes. Browser-Console/Page-Errors und nicht erlaubte externe Hosts sind leer.
- Der Windows-Browser protokolliert Kaspersky Web Anti-Virus am Host `gc.kis.v2.scr.kaspersky-labs.com`. Der Audit zeichnet ihn separat als Umgebungsrequest auf; alle anderen externen Hosts bleiben ein Fehler.

## Implementierte Korrektur

- `tests/Test-JobAgentUiBrowserAudit.ps1` klassifiziert externe Hosts: nur `127.0.0.1:8500` und dokumentierte Kaspersky-Telemetrie sind zulässig; jeder weitere Host lässt den Audit fehlschlagen.
- Die Evidence enthält nun `environmental_external_hosts` und `unexpected_external_hosts` zusätzlich zur vollständigen Requestliste.

## Nächster Arbeitsauftrag: QA-005.1

1. `tests/fixtures/jobagent/QA-005-visual-contract.json` anlegen: Pflichtzustände, Messwerte und begründete Ausnahmen für 390x844, 800x1024, 1366x768 und 1920x1080.
2. `Test-JobAgentHtmlAudit.ps1`, `Test-JobAgentHtmlViewportAudit.ps1` und gegebenenfalls `Test-JobAgentUiBrowserAudit.ps1` um geometrische Messungen erweitern: Root-Overflow, Boundingboxes, Überlappungen, Clipping, Mindestgröße 44 CSS-px, 200-%-Zoom.
3. Erst fokussierte Funktionstests ausführen; keinen Supertest starten, solange er nicht ausdrücklich angefordert wird.

## Offener technischer Befund

- `./ci.cmd self-check` ist weiterhin rot wegen `immutable_modified: manual\PROGRAM.md`. Das ist der offene Todo `TD-0056`; keine Pin-/Immutable-Reparatur ohne separaten Auftrag.
