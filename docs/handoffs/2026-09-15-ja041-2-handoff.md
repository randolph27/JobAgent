# Handoff: JA-041.2 abgeschlossen, JA-041.3 als naechster Slice

Stand: 2026-09-15T14:39:46.8787542+02:00

## Aktiver Roadmap-Punkt

- `TD-0053` / `JA-041` bleibt `in-progress`.
- `JA-041.1` und `JA-041.2` sind erledigt.
- `JA-041.3 Berufsneutrale Pipeline durchgaengig verifizieren` ist der einzige naechste Arbeitsschnitt. `UI-001` und `JA-042` duerfen erst danach beginnen.
- Keine Roadmap-Rotation: Der Oberpunkt `JA-041` hat mit `JA-041.3` noch einen offenen Unterpunkt.
- `supertest` wurde nicht angefragt und ist deshalb fuer diesen und den folgenden Abschluss kein offenes Gate. Er wurde nicht ausgefuehrt.

## Gelieferter Zustand aus JA-041.2

- `src/JobAgent.Classification.psm1` trennt drei Entscheidungen:
  - `Get-JobAgentOfficialJobValidity`: `VALID`, `REJECTED`, `UNKNOWN`; fehlender Titel, fehlende absolute Detail-URL und explizite `NAVIGATION`/`FAQ`/`NEWS`/`ARTICLE`/`LANDING_PAGE`/`CATEGORY`-Kennzeichen werden abgelehnt.
  - `Get-JobAgentRegionalScope`: `IN_SCOPE`, `OUT_OF_SCOPE`, `UNKNOWN`, ausschliesslich aus dem Stellenort.
  - `Get-JobAgentLeadershipClassification`: optionale IT-Fuehrungs-Profilpassung ohne Einfluss des Gebiets auf das Persistieren der Stelle.
- `src/JobAgent.DailyRun.psm1` schreibt diese drei Bewertungen an jeden normalisierten Rohjob.
- `src/JobAgent.StatusMachine.psm1` persistiert `job_validity` und `regional_scope`, nimmt sie bei Updates in `changed_fields` auf und verwirft explizit ungueltige Rohjobs.
- `src/JobAgent.Persistence.psm1` migriert Altjobs additiv: fehlende Felder werden als `UNKNOWN` mit historischem Hinweis ergänzt. Das veraendert weder `first_seen` noch den Lebenszyklusstatus.
- `schemas/jobagent.schema.json` verlangt beide neuen Objektfelder. Das bestehende Feld `classification` bleibt der optionale Profilscore.

## Nachweise

- Akzeptanzartefakt: `logs/jobagent/JA-041-2-acceptance.json`.
- Erfolgreiche Funktionstests:

```powershell
pwsh -NoProfile -File .\tests\Test-JobAgentClassification.ps1
pwsh -NoProfile -File .\tests\Test-JobAgentStatusMachine.ps1
pwsh -NoProfile -File .\tests\Test-JobAgentPersistence.ps1
pwsh -NoProfile -File .\tests\Test-JobAgentDailyRun.ps1
pwsh -NoProfile -File .\tests\Test-JobAgentSchema.ps1
```

- `git diff --check` war erfolgreich.
- Keine sichtbare Änderung: Browser-/Device-Lane ist nicht anwendbar.

## Auftrag fuer JA-041.3

1. In den bestehenden Tests isolierte Fixtures fuer IT-Leitung, Buchhaltung, Pflege, Ausbildung und eine explizite Nicht-Stelle ergänzen. Jede Rolle mit `MUNICH`, `FREISING`, `OUT_OF_SCOPE` und `UNKNOWN` abdecken; IDs, `job_validity`, `regional_scope`, Profilresultat und persistierte Menge exakt prüfen.
2. Zwei Läufe auf demselben Store abnehmen: Profilwechsel darf weder `NEW`, `CLOSED` noch `REMOVED` erzeugen. Ein Teilscan oder Quellenfehler darf keine Abwesenheit behaupten. Die Profiltrefferzahl und die vollständige Erfassungsmenge getrennt ausweisen.
3. Ein Runmanifest mit Suchscope, Vollständigkeitsgrenzen und Testergebnis erstellen; `logs/jobagent/JA-041-3-acceptance.json` mit Eingaben, Ergebnissen und Exitcodes erzeugen. Danach `JA-041` insgesamt abhaken, in das Roadmap-Archiv verschieben und Todo/Handoff/STP aktualisieren. Kein Supertest erforderlich, sofern nicht ausdrücklich angefordert.

## Grenzen

- Keine reale Firmenmengen- oder Live-Recherche als Abnahmebedingung.
- Keine Bewerbungen, Gebietsaufweitung, Frameworkwechsel oder ATS-Neuentwicklung.
- MSG-/ATS-Einzelfehler nur behandeln, wenn sie die neue allgemeine Pipeline nachweislich blockieren.
