# Übergabe – SQ-007 abgeschlossen, SQ-008 aktiv

Stand: 2026-09-17T14:45:14.247+02:00

## Aktiver Auftrag

- Todo: `TD-0069`, Status `in-progress`.
- Roadmap: `SQ-008 SonarQube-Analysevertrag, Fehlergate und Betriebsnachweis in die CI überführen`.
- Ziel des nächsten Arbeitsschnitts: Den getrennt getesteten Importpfad in `./ci.cmd sonar` überführen und dabei bei jeder fehlenden Voraussetzung fail-closed abbrechen.

## Abgeschlossener Vorpunkt SQ-007

- `TD-0068` und SQ-007 sind abgeschlossen und vollständig nach `Roadmap_archive.md` rotiert.
- SonarQube-Projekt: `jobagent-external-powershell`.
- Analyseumfang: ausschließlich `external-powershell-issues-only`; Scannerquellen sind `.ci/bin` und `tools`.
- Letzter erfolgreicher Import: 323 PSScriptAnalyzer-External-Issues; Compute-Engine-Task `AaCvZMBIOhdKqVTDjfxg`, Analyse `AaCvZMFMotDYp2WltX3c`, terminaler Status `SUCCESS`.
- Sekretfreie Laufzeit-Evidence: `logs/verify/sq-007-20260917-143744.json` und ein späterer erfolgreicher Wiederholungslauf unter `logs/verify/`.
- Versionierte Abnahme: `docs/reviews/SQ-007-external-issues-acceptance.md`.

## Implementierter Importvertrag

- `./ci.cmd sonar-external-import` ist der alleinige SQ-007-Importcommand.
- `.ci/bin/modules/sonar-external-issues.ps1` validiert Paket-, Einstiegspunkt- und Scanner-JAR-Hashes, erstellt den UTF-8-Report ohne BOM und schränkt Pfade fail-closed ein.
- `.ci/bin/modules/ci-commands-main.ps1` authentifiziert vor dem Import, erstellt das fest konfigurierte Projekt nur bei Fehlen, startet Java 11 und SonarScanner lokal und pollt den Compute-Engine-Task bis `SUCCESS`, `FAILED`, `CANCELED` oder Timeout.
- Der Token wird nur über die Child-Umgebung übergeben; er darf weder als Scannerargument noch in Dateien, Logs, Evidence, Handoff oder Git-Diff erscheinen.
- Es gibt keine Aussage zu nativer PowerShell-Analyse, Coverage, Duplikation, Quality-Profile-Regeln oder einer Quality-Gate-Gesamtfreigabe.

## Nachgewiesene Funktionstests

Alle folgenden Befehle endeten mit Exit 0:

```powershell
pwsh -NoProfile -File .\tests\Test-SonarToolchain.ps1
pwsh -NoProfile -File .\tests\Test-SonarExternalIssues.ps1
pwsh -NoProfile -File .\tests\Test-SonarAuth.ps1
pwsh -NoProfile -File .\tests\Test-JobAgentCiContracts.ps1
.\ci.cmd sonar-external-import
```

`Test-SonarExternalIssues.ps1` deckt leeren Report, unbekannte Regel, Pfad außerhalb des Roots, ausgeschlossene Pfade, ungültige Zeilen, Deduplizierung, UTF-8 ohne BOM, tokenfreie Scannerargumente und den erwarteten lokalen Scannerfehler ab. Der Vollsupertest wurde nicht angefragt und gilt als erledigt.

## SQ-008 – nächster konkreter Schnitt

1. `Cmd-Sonar` von `not-supported` auf den vorhandenen Import-Lifecycle umstellen. Der bestehende Gradle-/Quality-Gate-Pfad darf für den PowerShell-External-Issue-Betrieb nicht verwendet werden.
2. `tests/Test-SonarCiLifecycle.ps1` mit vollständig gemockter API-, Scanner- und Task-Grenze erstellen. Erfolg sowie fehlende Toolchain, Tokenformatfehler, invalider Report, Scannerfehler, Compute-Engine-Fehler und Timeout müssen getrennt mit stabiler Fehlerklasse geprüft werden.
3. Erfolgs- und Fehlerfälle in `logs/verify/sq-008-<run-id>.json`, Verify-Digest und Handoff sekretfrei dokumentieren.
4. Erst danach `./ci.cmd sonar` gegen `localhost:9000` ausführen, Ergebnis API-seitig gegen Projekt-Key und Analyse-ID abgleichen und SQ-008 nur bei vollständigem Nachweis rotieren.

## Grenzen

- Keine globale Toolinstallation, keine Änderungen an SonarQube-Quality-Gates oder -Profiles, kein automatischer Commit oder Push durch den Sonarcommand.
- Keine Analyse von `data/`, `logs/`, `.git`, Caches oder Tests.
- Die lokal gepinnte Toolchain liegt ignoriert unter `.ci/tools/sonar`; nur Hashpins und Code gehören in Git.
