# Roadmap

Stand: 2026-09-17. Ein aktiver Testinfrastrukturpunkt.

- [ ] QA-007 Deterministischen Startvertrag des Vollsupertests wiederherstellen #comment: Der beauftragte Lauf `./ci.cmd supertest` brach beim ersten geplanten Fall `QA-006-CI-CONTRACT` ab. Der Runner meldete für `pwsh -NoProfile -File tests/Test-JobAgentCiContracts.ps1` Exit-Code 5, ohne Standardausgabe oder -fehler, und führte die verbleibenden 28 von 29 Fällen nicht aus. Die Ursache ist noch nicht gesichert; dieser Punkt begrenzt die Analyse auf reproduzierbare Vertrags- und Runnerpfade und verlangt einen vollständig nachweisbaren grünen Vollsupertest.
  - [ ] Beschreibung: Die Testausführung muss den CI-Vertragsfall aus derselben Arbeitsumgebung wie der Supertest zuverlässig starten, dessen Fehlerdiagnostik ohne Geheimnisse erhalten und nach einer minimalen, ursachenbelegten Korrektur alle geplanten Fälle ausführen. Ein isoliert grüner Einzeltest genügt nicht, weil der Fehler im orchestrierten Ausführungspfad auftrat.
  - [ ] Scope: In Scope sind ausschließlich `tests/Test-JobAgentCiContracts.ps1`, `tests/JobAgent.Supertest.psm1`, `tests/Test-JobAgentSupertest.ps1`, `tests/Test-JobAgentSupertestContract.ps1`, die von diesen Tests direkt verwendeten CI-Module sowie die zugehörigen anonymisierten Testnachweise. Produktlogik, SonarQube-Projektkonfiguration, Zugangsdaten und nicht beteiligte Tests bleiben unverändert, sofern eine unmittelbare Kausalität nicht nachgewiesen wird.
  - [ ] Ist-Stand (2026-09-17 11:14 CEST): `./ci.cmd supertest` plante 29 Fälle, meldete `passed=0`, `failed=1`, `blocked=0`, `not_run=28` und beendete sich mit Exit-Code 1. Der erste Kindprozess `QA-006-CI-CONTRACT` erhielt Exit-Code 5 bei leerem stdout/stderr; damit ist weder ein fachlicher Vertragsfehler noch ein Runner-, Arbeitsverzeichnis-, Host- oder Fehlerweitergabeproblem behauptet.
  - [ ] Screenshot-Referenz: TODO: Der Nutzer hat am 2026-09-17 ein Kaspersky-System-Watcher-Fenster zu `d:\_script...\tests\test-jobagentcicontracts.ps1` beigefügt. Das Bild liegt dem Agenten nicht als Workspace-Datei vor und kann daher nicht ohne Erfindung unter `doc/roadmap-screenshots/` gespeichert werden. Vor einem erneuten Vollsupertest ist es dort unter einem mit `QA-007-` beginnenden Dateinamen abzulegen und als Hinweis auf einen externen Security-Blocker zu prüfen; weder Ausnahme noch Desinfektion sind Teil dieses Punkts.
  - [ ] Abhängigkeiten, Aufwand, Dauer, Prioritätsscore, Ordnungsbegründung, Risiken, Meilenstein, Parallelisierung: Keine fachliche Abhängigkeit; Aufwand M (2–5 Stunden), Dauer M, Prioritätsscore 100/100, weil der Fehler sämtliche nachgelagerten Qualitätssignale maskiert. Meilenstein: „verlässlicher CI-Qualitätsnachweis“. Nicht parallelisieren: Einzeltest, Runnerdiagnostik und Vollsupertest müssen gegen denselben Arbeitsstand erfolgen. Risiko: Eine bloße Unterdrückung des Exit-Codes würde einen echten Vertragsfehler verdecken; deshalb sind Fehlerursache und Ergebnisdatei vor Abschluss abzugleichen.
  - [ ] Schritte:
    1. [ ] Den exakten Kindprozess `pwsh -NoProfile -File .\tests\Test-JobAgentCiContracts.ps1` aus dem Repository-Stamm reproduzierbar ausführen, Exit-Code, strukturierte Fehlerdaten und die durch den Test aufgerufene Invariantenprüfung erfassen; sensible Werte, insbesondere Sonar-Tokens, bleiben aus allen Nachweisen ausgeschlossen.
    2. [ ] Die Einzelprozessausführung mit der Invocation in `tests/JobAgent.Supertest.psm1` und den Runner-Vertragstests vergleichen: Arbeitsverzeichnis, PowerShell-Argumente, `$ErrorActionPreference`, Exit-Code-Weitergabe, temporäre Ergebnisdatei und stdout/stderr-Erfassung müssen jeweils konkret verifiziert werden, um die Fehlerklasse deterministisch einzugrenzen.
    3. [ ] Nur die nachweislich ursächliche Vertrags- oder Runnerstelle minimal korrigieren, danach in dieser Reihenfolge den direkten CI-Vertragstest, den engsten betroffenen Runner-/Vertragstest und den vollständigen Supertest ausführen; Abschluss nur bei `planned=29`, `passed=29`, `failed=0`, `blocked=0`, `not_run=0` und Exit-Code 0.
  - [ ] Evidence: Versionierter Nachweis dokumentiert Ausgangsfehler, reproduzierenden Einzeltest, betroffenen Runnerpfad, korrigierte Ursache und die anonymisierte Vollsupertest-Zusammenfassung mit allen Zählern. Flüchtige Laufzeitlogs bleiben unversioniert; keine Zugangsdaten oder Tokenfragmente werden abgelegt.
  - [ ] Funktionstest: Zuerst ausschließlich `tests/Test-JobAgentCiContracts.ps1` und genau der diagnostisch betroffene Supertest-Runner-/Vertragstest. Jeder Lauf muss Prozessende, erwartete Anzahl und Exit-Code prüfen; eine Reparatur ohne diesen fokussierten Nachweis wird nicht akzeptiert.
  - [ ] Audit: Nicht anwendbar, weil kein visueller Produktfluss, UI-Layout oder Emulatorverhalten betroffen ist. Der manuelle Audit besteht stattdessen aus der prüfbaren Gegenüberstellung von Runner-Invocation, Ergebnisdaten und Exit-Code.
  - [ ] Supertest: Explizit beauftragt und daher zwingend. Erst nach erfolgreichen fokussierten Funktionstests `./ci.cmd supertest` vollständig ausführen; bei erneutem Fehler bleibt QA-007 offen und der reproduzierbare Fehlernachweis wird im Handoff festgehalten.

SQ-002 ist nach `Roadmap_archive.md` rotiert.

