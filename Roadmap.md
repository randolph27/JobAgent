# Roadmap

Stand: 2026-09-17. SQ-007 ist abgeschlossen: Der projektlokale, gepinnte Scanner importierte 323 PowerShell-External-Issues nach `jobagent-external-powershell`; der Compute-Engine-Task endete mit `SUCCESS`. SQ-009 blockiert den realen Abschluss von SQ-008: `./ci.cmd sonar` endete am 2026-09-17 14:56 CEST nach erfolgreichem Scannerstart im Compute-Engine-Read mit `sonar_external_compute_engine_lookup_failed`.

- [ ] SQ-009 Compute-Engine-Task-Read des kanonischen Sonar-Lifecycles deterministisch diagnostizieren und absichern #comment: Der Scanner erzeugt einen Server-Task, doch dessen API-Read wird derzeit in eine stabile Fehlerklasse überführt, ohne die technische Ursache, Task-ID oder den API-Status sekretfrei zu unterscheiden.
  - [ ] Beschreibung: `./ci.cmd sonar` muss nach einem erfolgreichen Scannerlauf den zugehörigen Compute-Engine-Task ausschließlich über die dokumentierte SonarQube-API lesen und bei HTTP-, Berechtigungs-, Antwortformat-, Task-ID-, Terminalstatus- und Timeoutfehlern jeweils eine eindeutige Fehlerklasse mit sekretfreier Diagnose schreiben. Akzeptanz: Für den erfolgreichen Lauf enthält die Evidence `project_key`, `analysis_scope`, `report_issue_count`, `task_id`, `analysis_id`, `task_status` und 40-stellige Commitreferenz; für jeden Fehler enthält sie mindestens `stage`, `error_class`, `task_id` soweit bereits bekannt, HTTP-Status soweit verfügbar und keinen Token, Header, Queryparameter, Tokenlängen- oder Scannerdebugwert.
  - [ ] Scope: `.ci/bin/modules/ci-commands-main.ps1`, `tests/Test-SonarCiLifecycle.ps1`, gegebenenfalls die ausschließlich für den Task-Read zuständige Sonar-API-Hilfsfunktion, `.ci/ci.config.json`, `docs/reviews/SQ-009-compute-engine-read-acceptance.md`, Roadmap/Todo/Handoff-Evidence. No-Gos: keine Änderung von SonarQube-Projekten, Quality Gates, Quality Profiles, Tokens, Serverkonfiguration, Scanner-/Java-/PSScriptAnalyzer-Pins, Analysequellen außerhalb `.ci/bin` und `tools`, globalen Toolinstallationen oder automatischen Commit-/Push-Auslösungen durch `sonar`.
  - [ ] Ist-Stand (2026-09-17 14:56 CEST): Der fokussierte Lifecycle-Test deckt die gemockte Fehlerklasse `sonar_external_compute_engine_lookup_failed` ab. Der reale Aufruf `./ci.cmd sonar` beendete sich mit Exit 1; `logs/verify/sq-007-20260917-145613.json` belegt `cmd:.\\ci.cmd sonar`, `analysis_scope:external-powershell-issues-only`, `stage:compute_engine`, `error_class:sonar_external_compute_engine_lookup_failed`, `exit:1`, `secret_sanitized:true`. Die konkrete HTTP-Ursache, Serverantwort und betroffene Task-ID sind nicht belegt und dürfen nicht geraten werden.
  - [ ] Abhängigkeiten: SQ-006-Toolchain und SQ-007-Import sind abgeschlossen; SQ-008 bleibt bis zum erfolgreichen, API-seitig bestätigten Compute-Engine-Read offen. Der reale Server `localhost:9000`, die bestehende lokale Tokenquelle und das bereits angelegte Projekt `jobagent-external-powershell` müssen unverändert erreichbar bleiben.
  - [ ] Aufwand/Dauer: S–M (0,5–1 PT / 4–8 Stunden) bei einer Person und verfügbarer lokaler SonarQube-Instanz. Prioritätsscore: 99/100. Ordnungsbegründung: Der Fehler liegt auf dem kritischen Pfad zwischen Scannerabschluss und belastbarer Analyse-ID; ohne ihn darf SQ-008 nicht als erfolgreicher Sonar-Lifecycle ausgewiesen werden.
  - [ ] Risiken und Unsicherheiten: SonarQube Server 9.9 kann einen Task nach Scannerende verzögert bereitstellen, Zugriff auf `/api/ce/task` anders autorisieren als Projektreads oder eine nicht dokumentierte Antwortform liefern. Keines dieser Szenarien rechtfertigt Retry ohne Fehlerklassifikation, Überspringen des API-Reads, Umdeutung eines Scanner-Exitcodes als Analyseerfolg oder eine Quality-Gate-Aussage für External Issues.
  - [ ] Schritte:
    1. Den vorhandenen `compute_engine`-Fehlerpfad mit einer reinen Read-only-API-Diagnose erweitern: pro Request Task-ID, Endpointklasse ohne Querywert, HTTP-Status, transport-/Timeoutklasse und die strukturgeprüften, nicht geheimen Taskfelder erfassen; dabei die API-Hilfsfunktion in `.ci/bin/modules/ci-commands-main.ps1` so begrenzen, dass Authentifizierungsdaten nur im Header der Laufzeit bleiben und nie in Evidence oder Fehlermeldungen gelangen.
    2. `tests/Test-SonarCiLifecycle.ps1` um vollständig gemockte Fälle für HTTP 401, 403, 404, 5xx, Transporttimeout, fehlendes `task`-Objekt, fehlenden oder ungültigen terminalen `analysisId`, `FAILED`, `CANCELED`, `PENDING` bis Timeout und `SUCCESS` erweitern; je Fall die exakte Fehlerklasse, den nichtnull Exit über den Commandvertrag und die sekretfreie Evidence-Form deterministisch prüfen.
    3. Nach grünen Funktionstests `./ci.cmd sonar` einmal gegen `localhost:9000` ausführen, danach ausschließlich per SonarQube-API die in Evidence referenzierte Task-ID und Analyse-ID abgleichen; `docs/reviews/SQ-009-compute-engine-read-acceptance.md` mit Command, Zeit, Commit, Projekt-Key, Scope, Befundzähler, terminalem Taskstatus, redigierter API-Read-Klasse und der expliziten Nichtaussage zu nativer PowerShell-Analyse, Coverage und Quality Gate erstellen.
  - [ ] Evidence: `logs/verify/sq-009-<run-id>.json` oder eine versionierte, auf SQ-009 umbenannte Folgeevidence mit den oben definierten Feldern; `docs/reviews/SQ-009-compute-engine-read-acceptance.md`; sekretfreie Fehlerproben aus `tests/Test-SonarCiLifecycle.ps1`; aktualisiertes Handoff mit genau einer referenzierten, erfolgreichen Analyse-ID. Verboten: Token, Basic-Header, URL-Querywerte, Tokenlängen, kompletter Responsebody, Scanner-Stdout/Stderr und Benutzerprofilpfade.
  - [ ] Funktionstest: `pwsh -NoProfile -File .\tests\Test-SonarCiLifecycle.ps1`, `pwsh -NoProfile -File .\tests\Test-SonarExternalIssues.ps1`, `pwsh -NoProfile -File .\tests\Test-SonarAuth.ps1`, `pwsh -NoProfile -File .\tests\Test-SonarToolchain.ps1` und `pwsh -NoProfile -File .\tests\Test-JobAgentCiContracts.ps1` jeweils Exit 0; anschließend `./ci.cmd sonar` mit terminalem `SUCCESS` und API-Abgleich der Evidence-IDs. Supertest: gemäß Nutzerregel erst nach Abschluss des Roadmap-Punkts und nicht als Ersatz für diesen Funktionsnachweis.
  - [ ] Audit: Statischer Review der geänderten API- und Evidencepfade auf fail-closed Verhalten, analysierten Quellsatz ausschließlich `.ci/bin,tools`, keine Projekt-/Gate-Mutation und keine Secrets. Browser-, Viewport- und Android-Audit: not-applicable, da ausschließlich CI-/SonarQube-Serverkommunikation verändert wird.
  - [ ] Meilenstein/Parallelisierung: S3.1 – belegter Compute-Engine-Read. Die gemockten Testfälle und die Evidencedokumentvorlage sind parallel möglich; echte API-Diagnose, realer Lauf und SQ-008-Abschluss erfolgen wegen derselben Task-/Analyse-ID sequenziell.

- [ ] SQ-008 SonarQube-Analysevertrag, Fehlergate und Betriebsnachweis in die CI überführen #comment: Erst nach belegtem External-Issue-Import darf der bisherige `not-supported`-Pfad durch einen fail-closed, eindeutig begrenzten Analysecommand ersetzt werden.
  - [x] Teilergebnis (2026-09-17): SQ-007 importierte einen hashbelegten Report mit 323 External Issues. Projekt-Key `jobagent-external-powershell`, Task `AaCvX7pnOhdKqVTDjfxf`, Analyse `AaCvX78SotDYp2WltXne`, Status `SUCCESS`; Evidence: `logs/verify/sq-007-20260917-143744.json`.
  - [ ] Beschreibung: `./ci.cmd sonar` führt den freigegebenen lokalen Ablauf aus oder endet bei jeder fehlenden Voraussetzung mit stabiler Fehlerklasse. Der Command unterscheidet Toolchain, Reportvalidierung, Authentifizierung, Projektzugriff, Scanner, Compute-Engine-Timeout und Importfehler. Handoff, Verify-Digest und Review-Evidence enthalten Analyseumfang, Analyse-ID, Commitreferenz, Taskstatus und Befundzähler, niemals Geheimnisse.
  - [ ] Scope: `.ci/bin/modules/ci-commands-main.ps1`, Sonar-Helper, `.ci/ci.config.json`, `tests/Test-SonarCiLifecycle.ps1`, CI-Vertrag, Testmatrix, `docs/reviews/SQ-008-ci-lifecycle-acceptance.md`, Roadmap/Todo/Handoff-Evidence. No-Gos: keine automatische Änderung von Quality-Gates/-Profiles, keine globale Toolinstallation, keine Analyse unfreigegebener Pfade und keine automatische Push-/Commit-Auslösung durch `sonar`.
  - [ ] Ist-Stand (2026-09-17 14:56): `sonar` ist auf den begrenzten External-Issue-Lifecycle umgestellt; `tests/Test-SonarCiLifecycle.ps1` deckt Erfolg sowie Server-, Auth-, Toolchain-, Report-, Projekt-, Scanner- und Compute-Engine-Fehlerklassen mit gemockten Grenzen ab. Die fünf fokussierten Sonar-Funktionstests endeten mit Exit 0. Der reale Command endete jedoch nach Scannerlauf im `compute_engine`-Read mit Exit 1 und `sonar_external_compute_engine_lookup_failed`; Evidence: `logs/verify/sq-007-20260917-145613.json`.
  - [ ] Abhängigkeiten: SQ-006 und SQ-007 sind abgeschlossen. SQ-009 hat Vorrang und muss die konkrete Compute-Engine-API-Ursache sowie die sekretfreie Taskdiagnose abschließen; erst danach sind der reale Lauf, der Reviewnachweis und der Abschluss von SQ-008 zulässig.
  - [ ] Aufwand/Dauer: M (1–2 PT / 1–3 Kalendertage) einschließlich Fehlerproben und CI-Abnahme.
  - [ ] Prioritätsscore: 91/100. Ordnungsbegründung: Der CI-Einstieg wird zuletzt umgestellt, damit ein abgebrochener Scannerlauf nie als erfolgreiche Codeanalyse oder bloßer Servercheck in den Handoff gelangt.
  - [ ] Risiken und Unsicherheiten: External Issues sind auf Server 9.9 nicht wie native Regeln über Quality-Profile steuerbar; ein Quality Gate kann daher semantisch leer oder irreführend sein. Sein Status wird nur als technische Serverantwort protokolliert und niemals als Gesamtfreigabe des PowerShell-Codes ausgegeben.
  - [ ] Schritte:
    1. [x] `sonar` auf den begrenzten External-Issue-Lifecycle umstellen: Toolchain prüfen, lokalen Token lesen, Report erzeugen/validieren, Scanner starten, Task pollen, Analysemetadaten lesen und bei jedem Fehler mit stabiler Klasse sowie nichtnull Exit abbrechen.
    2. [x] CI-Lifecycle-Test mit vollständig gemockter Scanner-, Task- und API-Grenze erstellen; Erfolg, Auth-, Toolchain-, Report-, Projekt-, Scanner- und Compute-Engine-Fehler sowie Timeout getrennt abdecken.
    3. [ ] Nach Abschluss von SQ-009 den realen Analyseabnahmelauf gegen `localhost:9000` über `./ci.cmd sonar` wiederholen und Scope, Versionen, Commit, Analyse-ID, External-Issue-Zähler, Taskstatus sowie Nichtaussage zu nativer Analyse/Coverage/Quality-Gate dokumentieren.
  - [ ] Evidence: `docs/reviews/SQ-008-ci-lifecycle-acceptance.md`, `logs/verify/sq-008-<run-id>.json`, aktualisiertes Handoff mit sekretfreier Analysemetadatenreferenz und API-Read-Protokoll. Token, Header, Queryparameter, Tokenlänge und Scanner-Debugdump sind verboten.
  - [ ] Funktionstest: `pwsh -NoProfile -File .\tests\Test-SonarCiLifecycle.ps1`, `pwsh -NoProfile -File .\tests\Test-SonarExternalIssues.ps1`, `pwsh -NoProfile -File .\tests\Test-SonarAuth.ps1` und `pwsh -NoProfile -File .\tests\Test-JobAgentCiContracts.ps1` jeweils Exit 0; danach `./ci.cmd sonar` mit Analyse-ID und terminalem Compute-Engine-Status `SUCCESS`.
  - [ ] Audit: Review auf fail-closed Exitcodes, keine Secrets, globale Pfade oder unzulässige Quellabdeckung sowie keine Behauptung eines nativen PowerShell-Quality-Gates. Der Serverlauf wird API-seitig gegen Projekt-Key und Commit abgeglichen. Browser-, Viewport- und Android-Audit: `not-applicable`.
  - [x] Supertest: Auf ausdrückliche Nutzerregel als erledigt markiert, ohne Ausführung. Ein Vollsupertest ersetzt keinen SonarQube-Nachweis.
  - [ ] Meilenstein/Parallelisierung: S3 – CI-aktivierter, begrenzter SonarQube-Betrieb. Lifecycle-Fixtures können parallel zu SQ-007-Evidence vorbereitet werden; Commandumschaltung, realer Lauf und Abschluss-Supertest erfolgen sequenziell.
