# Roadmap

Stand: 2026-09-15. Auftrag dieses Schnitts: ausschliesslich den vorhandenen Supertest-Ausbau planen, Workflow synchronisieren, STP, Stage, Commit und Push; danach stoppen. Alle folgenden Punkte sind offen. Testimplementierung und Supertest-Ausfuehrung beginnen erst in einem Folgeauftrag.

## Planungsgrundlage und verbindliche Ausfuehrungsregeln

- Projekt: lokaler JobAgent mit PowerShell-Modulen unter `src/`, CLI unter `tools/` und generierten HTML-Berichten; Einstieg `.\ci.cmd`, Devserver `http://127.0.0.1:8500`. README-Allgemeinvertrag und tatsaechliche JobAgent-Commands gelten. Die CI enthaelt historische Android-Referenzen; eine eindeutige zentrale Profilbindung ist in der gelesenen Konfiguration nicht ausgewiesen. Keine native App oder neue Plattform wird daraus abgeleitet.
- Bestehender Supertest: `tests/Test-JobAgentSupertest.ps1` mit 21 expliziten Tests einschliesslich Browseraudit und Matrixpruefung. Weitere sieben `Test-JobAgent*.ps1` sind vorhanden, aber nicht direkt eingebunden: Kandidatenverifikation, FetchEnvironment, FetchErrorInspection, CiContracts, HtmlAudit, HtmlViewportAudit sowie der aggregierende Supertest selbst. Der Aggregator darf sich niemals selbst starten. Nicht eingebunden bedeutet keine Aussage ueber bestehende indirekte Abdeckung.
- Vollstaendigkeit bedeutet: 100 % des inventarisierten fachlichen Funktions-, CLI-, UI- und Fehlervertrags besitzen konkrete Testfall-IDs und Assertions. Das ist weder eine Behauptung ueber jede denkbare Eingabe noch ueber die Vollstaendigkeit realer Firmen-/Stellenbestaende. Lines-/Branches-Ziel jeweils mindestens 90 % fuer messbaren Produktcode; Messbarkeit und Nenner werden in QA-001 geklaert, fehlende Messwerte bleiben `not-supported`.
- Kapazitaetsannahme: ein Entwickler, 1 PT = 8 Nettoarbeitsstunden, 0,7 FTE = 3,5 PT/Woche, vorhandener Windows-Host und lokale Browsertoolchain. Projektgroesse: 15 vorhandene PowerShell-Produktmodule und 28 JobAgent-Testdateien einschliesslich Aggregator; keine belastbare Funktionszahl vor QA-001. Teamskalierung und Zieltermin sind nicht angegeben. Schaetzungen sind Planungsspannen: zusammen 15-24 PT, bei sequenzieller Bearbeitung etwa 5-8 Kalenderwochen einschliesslich Review; keine Zusage. Optionale Parallelisierung bezeichnet nur spaetere Arbeit an getrennten Dateien, keinen Auftrag fuer weitere Agents.
- Prioritaetsscores sind fachliche Planungswerte von 0 bis 100, keine gemessenen Kennzahlen. Reihenfolge: Voraussetzungen, Daten-/Quellensicherheit, kritischer Ablauf, sichtbare Bedienung, visuelle Abnahme, Zusammenfuehrung. M1 = QA-001; M2 = QA-002 und QA-003; M3 = QA-004 bis QA-006.
- Je Hauptpunkt folgen exakt drei nummerierte fachliche Unterpunkte unter `Schritte:`; die weiteren Checkboxen enthalten die README-Pflichtmetadaten. Alle drei Unterpunkte und die fokussierten Funktionstests muessen vor dem Hauptpunkt-Abschluss erfuellt sein. Erst danach einmal `.\ci.cmd supertest` fuer die abgeschlossene Funktion ausfuehren. Bei Fehlschlag Fall-ID und Testdatei isolieren, nur den betroffenen Funktionstest zur Fehlerbehebung wiederholen und erst nach dessen Erfolg den Abschlusslauf erneut starten. Kein Supertest nach jedem Edit oder allein fuer diese Planung.
- Neue Evidence-/Testpfade sind unten ausdruecklich als anzulegen bezeichnet. Bestehende Tests werden bevorzugt erweitert. Uhren, Seed, IDs, Locale, Zeitzone, Fixturehashes, Browser-/CLI-Version und Reihenfolge werden festgelegt. Jeder Lauf nutzt einen eigenen containment-geprueften Root; keine produktiven Datenmutationen, keine Live-Webrecherche, keine Installationen oder Downloads im Testlauf. Fehlende Voraussetzungen bedeuten `blocked` und einen Fehlerexit statt uebersprungener gruener Tests.
- Evidence pro Hauptpunkt: neu anzulegendes `logs/jobagent/<ID>/acceptance.json` und `function-tests.log`, pro Fall ID, Inputhash, Aktion, erwarteter/ermittelter Wert, Command, Exitcode, Start/Ende, Ziel, Version und Artefaktpfad; zusammengefasster versionierter Abschluss in neu anzulegendem `docs/reviews/<ID>-acceptance.md`. Lokale Logs sind ignoriert und allein kein dauerhaft verfuegbarer Nachweis. Keine Token-/Personendaten in Fixtures, Screens oder Logs.
- Nutzer-Screenshots liegen fuer diesen Auftrag nicht vor. Daher keine erfundenen Screenshotreferenzen. Spaetere bindende Auditbilder unter `doc/roadmap-screenshots/<ID>-<fall>-<breite>x<hoehe>.png` ablegen, als `Screenshot-Referenz:` im betreffenden Punkt ergaenzen und unveraendert mitarchivieren.
- `TD-0056` bleibt als bereits bekannter CI-Driftbefund erhalten. Keine Pins zur Gruenfaerbung erneuern. Sonar ist gemaess `.ci/ci.config.json` derzeit `not-supported`; Serverstatus auf 9000 ist keine Analyse. Kein Sonar-Ausbau und keine Gradle-/ADB-Lane fuer das vorhandene PowerShell-/HTML-Produkt. Ein Browser-Viewport ersetzt keinen Android-Geraetenachweis.

## M1 - Pruefbare Vollstaendigkeits- und Determinismusgrundlage


## M2 - Datenintegritaet und vollstaendiger Ausfuehrungsweg

## M3 - Bedienung, visuelle Abnahme und aggregierter Supertest

- [ ] QA-005 Layout, Lesbarkeit und Tastaturbedienung messbar abnehmen #comment: Das blosse Vorhandensein einer Screenshotdatei beweist weder fehlerfreies Layout noch barrierearme Bedienbarkeit.
  - [ ] Beschreibung: Daily- und Coverage-UI bestehen eine reproduzierbare geometrische, visuelle und Tastaturpruefung mit null unbegruendeten Overlaps, Clipping- oder Fokusfehlern. Screenshots sind Belege fuer gemessene Zustaende und werden tatsaechlich visuell gesichtet.
  - [ ] Scope: `tests/Test-JobAgentHtmlAudit.ps1`, `tests/Test-JobAgentHtmlViewportAudit.ps1`, `tests/Test-JobAgentUiBrowserAudit.ps1`; vorhandene Report-/Coverage-Styles nur bei reproduziertem Defekt. Neu anzulegen `tests/fixtures/jobagent/QA-005-visual-contract.json` fuer Messwerte/Zustaende. Keine automatische Referenzfreigabe, kein pauschales Ausblenden von Inhalten und keine Produktneugestaltung.
  - [ ] Ist-Stand (2026-09-15 18:23): Der UI-Browseraudit erzeugt Screenshots bei vier Breiten und 2200 px Hoehe; er prueft dort Existenz und Mindestdateigroesse. Das ist allein kein geometrischer Nachweis. Separate HTML-/Viewportaudits bestehen; genaue Ueberschneidung und vorhandene Assertions werden ueber QA-001 abgeglichen.
  - [ ] Abhaengigkeiten: QA-004-Daten-/Controlmatrix, lokale festgelegte Browser-/Fontversion. Aufwand/Dauer: 2-3 PT; etwa 3-5 Arbeitstage. Prioritaetsscore: 87/100. Ordnungsbegruendung: Visuelle Kriterien brauchen bereits korrekte stabile Inhalte und Bedienzustaende.
  - [ ] Risiken und Unsicherheiten: Native Selects, Schriftverfuegbarkeit, Subpixelrundung und Screenshotunterschiede. Gewaehlte Grenzwerte sind Projekt-Abnahmekriterien, keine behauptete Zertifizierung. Bestehendes horizontales Scrollen in Datentabellen bleibt zulässig; die gesamte Seite darf dadurch nicht seitlich ueberlaufen.
  - [ ] Schritte:
    1. [x] QA-005.1 - Geometrie fuer sichtbare Zustaende messen: Browser mit fixem Locale, UTC, DeviceScaleFactor 1 und deaktivierten Animationen auf 390x844, 800x1024, 1366x768 und 1920x1080 setzen; Datums-/Fontladezustand vor Messung abwarten. Fuer Initialansicht, komplexen Filter, Nulltreffer, Firmen ohne Stellen, letzte Seite und lange Titel/URLs Boundingboxes/scrollWidth/clientWidth erfassen. Rootbreite maximal Viewport + 1 CSS-px; kein abgeschnittener Text ohne erreichbaren vollstaendigen Inhalt, keine Ueberlappung verschiedener bedienbarer Controls, numerische Hoehe/Breite von Buttons/Inputs mindestens 44 CSS-px. Ausnahmen wie Inline-Links oder Tabellen-Scrollcontainer einzeln begruenden, keine globale Ausnahme. Mehrzeilige Labels und 200-%-Zoom bei Desktopbreite pruefen.
    2. [ ] QA-005.2 - Tastatur und semantische Rueckmeldung testen: Ganze Filter-/Tab-/Paginationreise ausschliesslich mit Tab/Shift+Tab, Enter/Space und den fuer vorhandene native Controls passenden Tasten durchlaufen. Fokus sichtbar und nach Filterwechsel/Reset/Seitenwechsel sinnvoll erhalten; kein verstecktes Panel im Fokusweg, keine Tastaturfalle. Rollen, zugaengliche Namen, Labelzuordnung, aria-selected/aria-controls und Trefferstatus pruefen; Inhalte nicht nur per Farbe unterscheiden. Kontrast fuer normale Texte mindestens 4,5:1, grosse Texte und relevante Control-/Fokusbegrenzungen mindestens 3:1 als festes Projektziel messen; Transparenz-/Hintergrundfaelle explizit berechnen statt CSS-Farbstrings zu vergleichen.
    3. [ ] QA-005.3 - Visuelle Referenzen und Fehlerprobe absichern: Je Pflichtzustand Screenshots mit Fall-ID/Breite/Hoehe unter neuem `logs/jobagent/QA-005/screens/` erzeugen und die vier Grundansichten beider Berichtstypen einzeln sichten; Ergebnis mit Bildpfad, Befund und Freigabegrund speichern. In einer isolierten defekten Rendererfixture jeweils Clipping, Overlap, zu kleinen Button und unsichtbaren Fokus injizieren; entsprechender Messcheck muss gezielt fehlschlagen. Referenzbilder erst nach bestandenen Assertions und Sichtung unter `doc/roadmap-screenshots/` versionieren, Hash festhalten und Punkt um Screenshot-Referenz ergaenzen. Bildabweichung nie durch automatisches Ueberschreiben der Baseline beheben; bei reiner Renderstreuung Ursache dokumentieren und nur fachlich begruendete tolerierte Bereiche zulassen.
  - [ ] Evidence: Allgemeiner Evidence-Vertrag plus Boundingbox-/Overflow-/Kontrast-/Fokuswerte pro Element/Fall/Viewport, Screenshotmanifest mit Hashes, sichtbare Befunde und vier erwartete negative Fehlerproben. Fehlende visuelle Sichtung = nicht abgeschlossen.
  - [ ] Funktionstest: `pwsh -NoProfile -File .\tests\Test-JobAgentHtmlAudit.ps1`, `pwsh -NoProfile -File .\tests\Test-JobAgentHtmlViewportAudit.ps1`, `pwsh -NoProfile -File .\tests\Test-JobAgentUiBrowserAudit.ps1`; Devserver nur ueber bestehende CI-Status-/Startbefehle. Negative Layoutfixtures werden innerhalb dieser Tests als erwartete Fehler behauptet, ohne die finale Erfolgsbewertung zu verfaelschen.
  - [ ] Audit: Null offene Pflichtverletzungen in beiden Berichtstypen und allen vier Viewports; Tabellen duerfen intern horizontal scrollen, Bedienelemente und Texte bleiben erreichbar. Android-Emulator `not-applicable` fuer diesen Webbericht-Vertrag; kein physischer Geraetenachweis wird behauptet.
  - [ ] Supertest: Nach fachlicher/visueller Abnahme und gruenen obigen Tests einmal `.\ci.cmd supertest`; bei Layoutfehlern nur den betroffenen Viewport-/Controlfall zur Reparatur wiederholen, dann das Abschlussgate.
  - [ ] Meilenstein/Parallelisierung: M3-Visualabnahme abgeschlossen; kann QA-006-Nachweisformat vorbereiten, aber gemeinsame Browser-/Screenshotroots nie parallel beschreiben.

- [ ] QA-006 Vorhandenen Supertest kontrolliert erweitern und seine Vollstaendigkeit nachweisen #comment: Der einzige Startbefehl soll alle abgeschlossenen Funktionalitaeten und die echte UI mit belastbarem Ergebnisprotokoll pruefen.
  - [ ] Beschreibung: `.\ci.cmd supertest` fuehrt jeden freigegebenen Matrix-Test genau einmal in festgelegter Reihenfolge aus, liefert auch bei Abbruch einen maschinenlesbaren Gesamtstatus und kann bei fehlenden/roten Pflichtfaellen niemals Exit 0 melden. Neu hinzugefuegte Produktfunktionen ohne Zuordnung werden automatisch erkannt.
  - [ ] Scope: `tests/Test-JobAgentSupertest.ps1`, `tests/Test-JobAgentTestMatrix.ps1`, `docs/test-matrix.json`, `docs/test-matrix.md`; neu anzulegen `tests/Test-JobAgentSupertestContract.ps1` fuer isolierte Runner-Fehlerfaelle. CI-Dispatcher nur bei belegtem Routingdefekt minimal anpassen, keine ungepruefte Runtime-/Pinreparatur und keine Einbindung fremder Bootstrap-/Android-Releasesuiten.
  - [ ] Ist-Stand (2026-09-15 18:23): Runner startet 21 Dateien seriell ueber pwsh, speichert acht Ausgabezeilen je Ergebnis und wirft beim ersten Nichtnull-Exit eine Exception. Vollstaendige Resultliste wird erst nach erfolgreicher Schleife ausgegeben; ein eigener Timeout-/Abbruch-/Restfaelle-Nachweis ist in diesem Runner nicht vorhanden. Letzter archivierter gruener Lauf ist historische Evidence, kein Test des geplanten Ausbaus.
  - [ ] Abhaengigkeiten: QA-001 bis QA-005 vollstaendig fachlich abgeschlossen und einzeln gruen; alle Testluecken geschlossen oder sachlich `not-applicable` mit Vertrag/Begruendung, keine offene Pflichtfunktion. Aufwand/Dauer: 2-3 PT; etwa 3-5 Arbeitstage. Prioritaetsscore: 84/100. Ordnungsbegruendung: Aggregation folgt bewusst zuletzt, da sie ungetestete Einzelkomponenten nicht korrigiert.
  - [ ] Risiken und Unsicherheiten: Langlaufende Teiltests, rekursive Aggregation, fehlender npm-/Browsercache, doppelte Matrixeintraege und veraltete Evidence. Grenzwerte fuer Laufzeiten erst aus fokussierten Messungen festlegen und versionieren; keine Performancebehauptung anhand eines Einzelrechners.
  - [ ] Schritte:
    1. [ ] QA-006.1 - Testauswahl und Reihenfolge zentralisieren: Den vorhandenen Runner erhalten und Testauswahl aus der in QA-001 erweiterten Matrix ableiten; nur freigegebene abgeschlossene Funktionstests, Contracttests und lokale UI-Lane ausfuehren. Direkte Luecken Kandidatenverifikation, FetchEnvironment, FetchErrorInspection, CiContracts, HtmlAudit und HtmlViewportAudit nach belegtem Einzel-Erfolg aufnehmen; Doppelabdeckung als Fallreferenz modellieren, gleiche Testdatei nicht mehrfach starten. Reihenfolge in der Matrix festlegen: Contract/Schema, Store/Domain, Quellen/Discovery, Daily/Betrieb/Reports, echte UI/Visual; Abhaengigkeiten topologisch pruefen. Self-Inclusion, unbekannte Dateien, zyklische Abhaengigkeit, leere Auswahl, fehlende Pflichtzuordnung oder offene Pflichtfaelle verhindern den Start mit eindeutiger Diagnose.
    2. [ ] QA-006.2 - Runnerfehler im Funktionstest pruefen: In neuem `Test-JobAgentSupertestContract.ps1` nur winzige isolierte Child-Skripte fuer Erfolg, Nichtnull-Exit, Exception, ungueltige Ergebnisdaten, Timeout und Abbruch verwenden; der Contracttest darf nie die echte Gesamtsuite rekursiv starten. Child-stdout/stderr, Exit, Start/Ende, Version, Arbeitsverzeichnis und Fehlerfall persistieren; nach erstem Fehler verbleibende Tests `not-run` mit Grund markieren und atomar Gesamtbericht schreiben. Fehlende Browser-/Servervoraussetzung ist `blocked` und Exit ungleich 0; erwartete Negativfixture ist nur dann gruen, wenn ihre konkrete Fehlerassertion stimmt. Cleanup darf nur eigene containment-gepruefte Tempdaten und nachgewiesene eigene Child-Prozesse betreffen.
    3. [ ] QA-006.3 - Vollstaendigen Abschluss mit Nachweiskette ausfuehren: Zuerst Matrix- und Runner-Contracttests gruen, danach neue Einzeltests entsprechend der Matrix. Erst wenn QA-006.1/.2 und die fachlichen Aenderungen abgeschlossen sind, `.\ci.cmd supertest` ausfuehren. Fuer den neuen Determinismusvertrag zwei komplette Laeufe in frischen isolierten Roots mit denselben Fixturehashes vergleichen; dieser zweite Lauf ist der begruendete Wiederholbarkeitstest, kein allgemeiner Dauerlauf. Fachliche Ergebnisse/Fallmengen sind gleich, nur freigegebene volatile Zeit-/Laufpfade unterscheiden sich. Gesamtstatus enthaelt Soll-/Ist-Testzahl, passed/failed/blocked/not-run, Inventory-/Matrix-/Quellhash, gemessene Lines/Branches mit Nenner oder wahrheitsgemaesses not-supported, Browser-/Visualstatus und separates Sonar-/Device-Ergebnis. Erst bei null offenen Pflichtfehlern archivieren, Todo/Checkpoint/Handoff synchronisieren und STP ausfuehren.
  - [ ] Evidence: Neu `logs/jobagent/QA-006/<run-id>/summary.json`, einzelne Childlogs, normalisierter Laufvergleich, aktuelle Inventar-/Matrixhashes und `docs/reviews/QA-006-acceptance.md` mit konkretem Commitbezug. Der versionierte Nachweis nennt die exakten Commands und Grenzen; historische Logs oder erreichbare Server reichen nicht.
  - [ ] Funktionstest: Neu `pwsh -NoProfile -File .\tests\Test-JobAgentSupertestContract.ps1`, danach `pwsh -NoProfile -File .\tests\Test-JobAgentTestMatrix.ps1` und `pwsh -NoProfile -File .\tests\Test-JobAgentCiContracts.ps1`; einzeln Exit 0. Fuer neue Aggregatoreintraege deren bereits festgeschriebene Einzelcommands verwenden, keine Supertestschleife zur Fehlerlokalisierung.
  - [ ] Audit: Gesamtbericht referenziert echte aktuelle UI-Interaktions-, Geometrie-, Tastatur- und Screenshot-Evidence fuer Daily/Coverage bei 390/800/1366/1920 px. UI-/Browserfehler, fehlende Sichtung oder unbegruendetes Skip verhindern Vollabnahme. Sonar bleibt explizit not-supported, bis ausserhalb dieses Auftrags eine passende Analyse eingerichtet ist.
  - [ ] Supertest: Zwei begruendete finale Aufrufe `.\ci.cmd supertest` nach fachlichem Abschluss und gruenen Funktionstests fuer QA-006.3; bei Fehler konkreten Einzeltest reparieren, dann den noch fehlenden Abschluss-/Vergleichsnachweis wiederholen. Niemals einen fehlenden Lauf als erledigt werten.
  - [ ] Meilenstein/Parallelisierung: M3 und Gesamtvorhaben enden mit vollstaendiger produktbezogener Abnahme ueber einen Einstieg. Runner-Contractfixtures nach M1 parallel entwerfbar; Matrixfreigabe, finale Browserlaeufe, STP und Git-Abschluss seriell.
