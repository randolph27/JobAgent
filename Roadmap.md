# Roadmap

## Annahmen und Priorisierungslogik

- Kapazitätsannahme: 1 Entwickler/Agent, lokale Windows-Umgebung `D:\_Scripte\JobAgent`, App läuft nur lokal, Daily-Runs zunächst manuell oder über lokale Automatisierung, kein externer Schreibzugriff auf Bewerbungs- oder Unternehmenssysteme.
- Projektgröße: PowerShell-Modulstack mit JSON-Store, CI-Vertrag, Funktions- und Supertest-Lane; keine Web-UI als Produktivserver, aber lokale HTML-Berichtsartefakte sind fachlich erforderlich.
- Review-Basis: Implementierung bis JA-022 ist weitgehend fachlich angelegt. Gesichert vorhanden sind Persistenz, Firmen-Seed, offizieller Quellenbeleg, Discovery-Import, Fixture-/generischer HTML-/Live-Adapter, Klassifikation, Deduplikation, Statusmaschine, Daily-Run, JSON-/Markdown-/HTML-Report, Betriebsstatus, Coverage-Backlog, lokaler HTML-Audit und Supertest-Lane.
- Kritische Lücken: Das produktive Firmeninventar enthaelt aktuell nur 38 Arbeitgeber. Fuer das Ziel "alle Muenchner und Freisinger Firmen" fehlt eine skalierbare Kandidatenbasis aus Register-, Regional- und Jobboersenquellen; Vollstaendigkeit, Lizenz-/Nutzungsgrenzen, Deduplikation, Standortbezug und offizielle Karriereverifikation sind noch nicht belastbar operationalisiert.
- Priorisierung: Zuerst muessen Quellenrecht, Source Registry, Rate-Limits und Evidenzvertrag stabil sein, danach werden massentaugliche Register-/Regional-/Jobboersen-Hinweise importiert, anschliessend werden Kandidaten dedupliziert und standortbezogen bewertet, danach werden offizielle Firmen-/Karriere-/ATS-Belege automatisiert verifiziert und erst dann wird der produktive Store in Wellen auf tausende Firmen erweitert.
- No-Go über alle Punkte: keine erfundenen Unternehmen, Stellen, URLs, Job-IDs, Geodaten, Gehälter oder Verifikationsaussagen; Jobbörsen nur zur Entdeckung, nicht als Primärnachweis; keine Bewerbung; keine extern wirksame Aktion ohne ausdrückliche Bestätigung; keine Secrets in Reports, Logs, Todo, Handoff oder Git.

## Aktive Punkte

- [ ] JA-034 Produktive Discovery-Snapshot-Lane fuer Muenchen/Freising aus erlaubten Quellen aufbauen #comment: Nach den technischen Discovery-, Dedupe- und Verifikationsgates muss der Kandidatenbestand reproduzierbar aus realen, erlaubten Quellen wachsen.
  - [ ] Beschreibung: Es existiert eine begrenzte, wiederholbare Snapshot-Lane, die reale Quellen aus `data/jobagent/company-discovery.sources.json` nur gemaess `allowed_use`, `rate_limit_policy`, `robots_or_terms_note` und `import_mode` verarbeitet und daraus lokale Kandidaten-Hints fuer Muenchen, 20-km-Umkreis und Freising erzeugt. Jeder Snapshot enthaelt Quelle, Abrufzeit, Query/Seed, Hash, Record-Zaehler, Reject-Gruende, Lizenz-/Nutzungsnotiz, Rate-Limit-Nachweis und `official_verification_required`; produktive Firmen oder JobSources werden nicht direkt geschrieben.
  - [ ] Scope: Betroffen sind voraussichtlich `tools/Import-JobAgentCompanyDiscovery.ps1`, `src/JobAgent.CompanyInventory.psm1`, `src/JobAgent.RegisterDiscovery.psm1`, `src/JobAgent.JobBoardDiscovery.psm1`, `src/JobAgent.RegionalDiscovery.psm1`, `data/jobagent/company-discovery.hints.json`, `logs/jobagent/company-discovery-snapshot-*.json`, `docs/company-discovery-operations.md` und Tests `tests/Test-JobAgentCompanyInventory.ps1`, `tests/Test-JobAgentRegisterDiscovery.ps1`, `tests/Test-JobAgentJobBoardDiscovery.ps1`, `tests/Test-JobAgentRegionalDiscovery.ps1`. No-Go: kein Scraping hinter Login/Captcha, keine Umgehung von Robots-/Terms-Grenzen, keine Jobboerse als offizieller Beleg, keine Vollstaendigkeitsbehauptung und kein produktiver Store-Write.
  - [ ] Ist-Stand (2026-08-24 09:05): Roadmap und Todo enthalten keine aktiven Punkte. Bisherige Infrastruktur bis JA-033 ist abgeschlossen; laut Roadmap-Annahmen enthaelt der produktive Store weiterhin nur 38 belegte Arbeitgeber. Ob aktuelle Live-Quellen erreichbar und nutzbar sind, ist noch pro Quelle zu pruefen.
  - [ ] Abhängigkeiten: JA-023 bis JA-030 muessen abgeschlossen bleiben; Source Registry und bestehende Importer sind Grundlage. JA-035 und JA-036 haengen von einem belastbaren Snapshot-Output ab.
  - [ ] Aufwand/Dauer: Aufwand L, Dauer 2-4 PT bei 1 Entwickler/Agent; Parser-/Fixturetests koennen parallel zur Dokumentation laufen, Live-Snapshot-Ausfuehrung und Nutzungsgrenzenpruefung nicht.
  - [ ] Prioritätsscore: 94/100, weil ohne reale, auditierbare Kandidaten-Snapshots die Firmenabdeckung nicht ueber den aktuellen kleinen Store hinauswachsen kann.
  - [ ] Ordnungsbegründung: Erst muss die erlaubte Kandidatenbasis reproduzierbar entstehen; Review, offizielle Verifikation und produktive Importwellen koennen nur auf belegten Snapshots aufsetzen.
  - [ ] Risiken und Unsicherheiten: Nutzungsbedingungen und Robots-Regeln koennen sich geaendert haben; einzelne Quellen koennen blockieren, rate-limitiert sein oder keine stabilen Daten liefern. Bei Unsicherheit muss die Quelle fail-closed als `MANUAL_REVIEW` oder `BLOCKED_BY_TERMS` markiert werden.
  - [ ] Schritte:
    1. Source-Auswahl und Snapshot-Contract pruefen: Nur Quellen mit erlaubtem `import_mode` fuer lokale Kandidaten-Hints aktivieren und fehlende Nutzungs-/Rate-Limit-Felder fail-closed ablehnen.
    2. Importer-Lane erweitern: Register-, Regional- und Jobboersen-Discovery so ausfuehren, dass jede Quelle ein separates Snapshot-Artefakt mit Hash, Zaehlern, Reject-Gruenden und Reviewpflicht schreibt.
    3. Merge in Hint-Store absichern: Neue Hints idempotent in `company-discovery.hints.json` uebernehmen, bestehende Hints nicht loeschen und Dubletten nur als Cluster-/Review-Information markieren.
    4. Funktionstests ergaenzen: Erlaubte, blockierte, rate-limitierte und unvollstaendige Quellen mit Fixture-Daten testen; Live-Abhaengigkeiten in Tests mocken oder als not-run dokumentieren.
  - [ ] Evidence: `logs/jobagent/company-discovery-snapshot-*.json`, aktualisierte `data/jobagent/company-discovery.hints.json`, aktualisierte Dokumentation `docs/company-discovery-operations.md`, Testlogs der betroffenen Discovery-Tests und ein Snapshot-Digest mit Quelle, Zaehlern, Hashes und Reject-Gruenden.
  - [ ] Funktionstest: `pwsh -NoProfile -File tests\Test-JobAgentCompanyInventory.ps1`; `pwsh -NoProfile -File tests\Test-JobAgentRegisterDiscovery.ps1`; `pwsh -NoProfile -File tests\Test-JobAgentJobBoardDiscovery.ps1`; `pwsh -NoProfile -File tests\Test-JobAgentRegionalDiscovery.ps1`.
  - [ ] Audit: Pruefen, dass Reports keine Vollstaendigkeit behaupten, dass Jobboersen nur Discovery-Hints erzeugen, dass jede Quelle Nutzungsgrenzen sichtbar macht und dass keine extern wirksame Aktion ausser erlaubtem Lesen ausgefuehrt wurde.
  - [ ] Supertest: `.\\ci.cmd supertest` erst nach gruenen Funktionstests; falls nicht ausdruecklich angefragt, gilt er gemaess Nutzeranweisung als erledigt.
  - [ ] Meilenstein: M8.1 Reale Discovery-Snapshots; parallelisierbar mit Dokumentationspflege, nicht mit produktiver Verifikation.

- [ ] JA-035 Kandidaten-Review-Queue und Coverage-Arbeitsbericht fuer Muenchen/Freising operationalisieren #comment: Viele Kandidaten bleiben nach Discovery unsicher; der Agent braucht eine priorisierte, nicht-destruktive Review-Queue statt unuebersichtlicher Rohdaten.
  - [ ] Beschreibung: Es existiert eine Review-Queue, die Discovery-Hints aus Register-, Regional- und Jobboersenquellen nach Evidenzklasse, Zielgebietsbezug, Dublettencluster, Verifikationsstatus, Freshness, Risiko und erwartetem Nutzen priorisiert. HTML-, Markdown- und JSON-Berichte zeigen klare naechste Aktionen (`VERIFY_OFFICIAL_SITE`, `CHECK_LOCATION`, `REJECT_DUPLICATE`, `WAIT_FOR_REFRESH`, `MANUAL_DECISION`) ohne Bewerbungs- oder Formularaktion.
  - [ ] Scope: Betroffen sind voraussichtlich `src/JobAgent.Coverage.psm1`, `src/JobAgent.CompanyInventory.psm1`, `tools/Measure-JobAgentCompanyCoverage.ps1`, `data/jobagent/company-candidate-verification.queue.json`, `html/jobagent/company-coverage.html`, `tests/Test-JobAgentCoverage.ps1`, `tests/Test-JobAgentCompanyDedupeScale.ps1` und `tests/Test-JobAgentHtmlAudit.ps1`. No-Go: keine automatische Loeschung unsicherer Hints, kein produktiver Upsert, keine Personen-/Kontaktdaten-Anreicherung, keine Bewerbung.
  - [ ] Ist-Stand (2026-08-24 09:05): Review- und Coverage-Mechaniken existieren aus JA-027 bis JA-030, aber es gibt keinen aktiven Roadmap-Punkt fuer eine neue priorisierte Arbeitsqueue aus realen Snapshots. Die aktuelle Kandidatenmenge und HTML-Queue-Darstellung sind vor Umsetzung zu messen.
  - [ ] Abhängigkeiten: JA-034 muss mindestens einen aktuellen Snapshot oder eine belastbare Fixture liefern; bestehende Dedupe- und Coverage-Tests muessen gruen sein. JA-036 haengt von den priorisierten Queue-Ausgaben ab.
  - [ ] Aufwand/Dauer: Aufwand M, Dauer 1-2 PT bei 1 Entwickler/Agent; Scoring-Regeln und HTML-Report koennen teilweise parallel bearbeitet werden.
  - [ ] Prioritätsscore: 86/100, weil eine skalierte Kandidatenbasis ohne priorisierte Review schnell unsteuerbar wird und falsche produktive Importe wahrscheinlicher macht.
  - [ ] Ordnungsbegründung: Nach Snapshot-Erzeugung kommt die Arbeitssteuerung; erst danach sollte eine offizielle Verifikationswelle auf Top-Kandidaten laufen.
  - [ ] Risiken und Unsicherheiten: Scoring kann kleine, relevante Arbeitgeber benachteiligen; unklare Standorte oder Konzernstrukturen duerfen nicht automatisch abgewertet oder verschmolzen werden. Score-Gruende muessen daher transparent im Bericht stehen.
  - [ ] Schritte:
    1. Review-Scoring definieren: Gewichtung fuer Quelle, Zielgebiet, Dublettencluster, Verifikationsbedarf, Freshness und Risiko in `src/JobAgent.Coverage.psm1` oder passendem Modul kapseln.
    2. Queue-Artefakte schreiben: `company-candidate-verification.queue.json` idempotent mit `next_action`, `priority_score`, `reason_codes`, `source_evidence` und `dedupe_context` aktualisieren.
    3. Arbeitsbericht rendern: HTML/Markdown um sortierte Review-Abschnitte, Filterzaehler, Reject-Gruende, sichere interne Links und responsive Tabellen erweitern.
    4. Regression absichern: Tests fuer Score-Stabilitaet, Queue-Idempotenz, unsichere Kandidaten, Dubletten und HTML-Audit ergaenzen.
  - [ ] Evidence: Aktualisierte Queue-Datei, `logs/jobagent/company-coverage-*.json`, `logs/jobagent/company-coverage-*.md`, `html/jobagent/company-coverage.html`, Testlogs fuer Coverage, Dedupe und HTML-Audit.
  - [ ] Funktionstest: `pwsh -NoProfile -File tests\Test-JobAgentCoverage.ps1`; `pwsh -NoProfile -File tests\Test-JobAgentCompanyDedupeScale.ps1`; `pwsh -NoProfile -File tests\Test-JobAgentHtmlAudit.ps1`.
  - [ ] Audit: Bei 1920/1366/800 px pruefen, dass Tabellen scrollbar statt abgeschnitten sind, keine Texte ueberlaufen, externe Quellen nur sichere Links sind und Review-Gruende ohne horizontales Layoutbrechen lesbar bleiben.
  - [ ] Supertest: `.\\ci.cmd supertest` erst nach gruenen Funktionstests; falls nicht ausdruecklich angefragt, gilt er gemaess Nutzeranweisung als erledigt.
  - [ ] Meilenstein: M8.2 Review-Queue fuer Kandidatenbetrieb; parallelisierbar mit JA-034 nur fuer UI-/Report-Arbeit auf Fixtures.

- [ ] JA-036 Begrenzte offizielle Verifikationswelle aus der Kandidaten-Queue ausfuehren #comment: Der produktive Store darf erst wachsen, wenn Top-Kandidaten ueber offizielle Firmen-, Karriere- oder ATS-Belege fail-closed verifiziert wurden.
  - [ ] Beschreibung: Eine begrenzte Verifikationswelle verarbeitet die priorisierten Top-Kandidaten aus JA-035, prueft offizielle Website-, Karriere- und ATS-Belege im erlaubten Umfang und schreibt nur bei belastbarer Evidenz `CAREER_URL_VERIFIED`, `COMPANY_DOMAIN_VERIFIED` oder `OFFICIAL_ATS_VERIFIED`. Alle anderen Kandidaten bleiben mit maschinenlesbarem Grund in Retry, Manual Review oder Reject; Jobboersenlinks werden nie zu offiziellen Anbieterlinks.
  - [ ] Scope: Betroffen sind voraussichtlich `tools/Verify-JobAgentCompanyCandidates.ps1`, `src/JobAgent.SourceVerification.psm1`, `src/JobAgent.LiveScan.psm1`, `src/JobAgent.CompanyInventory.psm1`, `data/jobagent/company-candidate-verification.queue.json`, `logs/jobagent/company-candidate-verification-*.json`, `tests/Test-JobAgentCompanyCandidateVerification.ps1`, `tests/Test-JobAgentSourceVerification.ps1` und `tests/Test-JobAgentLiveScan.ps1`. No-Go: keine Bewerbungen, kein Formular-Autofill, kein Login/Captcha, keine globale ATS-Allowlist ohne Firmenbeleg, keine Uebernahme ungesicherter Aggregator-URLs.
  - [ ] Ist-Stand (2026-08-24 09:05): Technische Verifikationslogik existiert aus JA-028, aber es gibt keinen neuen aktiven Punkt fuer eine konkrete begrenzte Welle auf aktuellen, priorisierten Kandidaten. Die Zahl der verarbeitbaren Kandidaten ist erst nach JA-034/JA-035 bekannt.
  - [ ] Abhängigkeiten: JA-034 und JA-035 muessen abgeschlossen oder mit belastbaren Fixture-/Snapshot-Artefakten verifizierbar sein; Source Registry muss jede Live-Quelle erlauben.
  - [ ] Aufwand/Dauer: Aufwand L, Dauer 2-3 PT bei 1 Entwickler/Agent; Fixture-Funktionstests koennen parallel zur Live-Wellenplanung laufen, Live-Ausfuehrung und Audit nicht.
  - [ ] Prioritätsscore: 78/100, weil Verifikation hohen Produktwert hat, aber erst nach Snapshot und Review-Queue fachlich sicher ist.
  - [ ] Ordnungsbegründung: Offizielle Verifikation ist das Gate vor produktiver Erweiterung und darf nicht vor priorisierter Kandidatensteuerung starten.
  - [ ] Risiken und Unsicherheiten: Webseiten koennen JavaScript-only, blockiert, mehrdeutig oder nicht erreichbar sein; Suchergebnisse koennen falsche Domains liefern. Unsichere Faelle muessen fail-closed bleiben und duerfen den Store nicht veraendern.
  - [ ] Schritte:
    1. Wellenlimit festlegen: Maximalzahl, Rate-Limits, Timeout, Retry-Regeln und erlaubte Quellen aus Registry und Queue ableiten.
    2. Verifikation ausfuehren: Top-Kandidaten pruefen, Redirect-Ketten, Evidence-Hashes, Statuscodes, finale URLs und Gruende protokollieren.
    3. Produktive Writes absichern: Nur offiziell verifizierte Kandidaten upserten; vor jedem Store-Write Backup und Gate-Report erzeugen.
    4. Tests und Audit erweitern: Positive und negative Verifikationspfade, ATS mit/ohne Firmenbeleg, Redirects, Timeout, 404 und Review-Pfade testen.
  - [ ] Evidence: `logs/jobagent/company-candidate-verification-*.json`, Store-Backup unter `data/jobagent/backups/`, aktualisierte Queue mit Verifikationsstatus, Coverage-Delta-Bericht, Testlogs der Verifikations- und LiveScan-Tests.
  - [ ] Funktionstest: `pwsh -NoProfile -File tests\Test-JobAgentCompanyCandidateVerification.ps1`; `pwsh -NoProfile -File tests\Test-JobAgentSourceVerification.ps1`; `pwsh -NoProfile -File tests\Test-JobAgentLiveScan.ps1`; bei Store-Write zusaetzlich `pwsh -NoProfile -File tests\Test-JobAgentCompanyInventory.ps1`.
  - [ ] Audit: Stichprobenartig pruefen, dass jede produktiv uebernommene Firma eine offizielle Evidence-URL hat, dass Jobboersen nicht als offizielle Links erscheinen und dass Reports keine externe Erreichbarkeit ueber den belegten Pruefzeitpunkt hinaus behaupten.
  - [ ] Supertest: `.\\ci.cmd supertest` erst nach gruenen Funktionstests; falls nicht ausdruecklich angefragt, gilt er gemaess Nutzeranweisung als erledigt.
  - [ ] Meilenstein: M8.3 Offizielle Verifikationswelle; nicht sinnvoll parallel zur finalen JA-035-Queue-Abnahme.
