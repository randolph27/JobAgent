# Roadmap

Stand: 2026-09-16. Ein aktiver technischer Punkt.

- [ ] CI-003 Projektlokale Playwright-Daemon- und Fehlerartefakte fuer die Browser-Lane erzwingen #comment: Der Abschluss-Supertest scheiterte am 2026-09-16 um 16:09 CEST nach 24/29 bestandenen Faellen, weil Playwright eine Daemon-Fehlerdatei im gesperrten Benutzerpfad `C:\Users\ralph\AppData\Local\ms-playwright\daemon\...err` anlegen wollte.
  - [ ] Beschreibung: `tests/Test-JobAgentUiBrowserAudit.ps1` und die von ihm gestartete Playwright-CLI schreiben Daemon-, Browser-, Fehler- und Temp-Artefakte ausschliesslich in eindeutig pro Lauf isolierte, projektlokale und ignorierte Pfade; der UI-Browsertest sowie der Vollsupertest laufen ohne Lese- oder Schreibzugriff auf `C:\Users\ralph\AppData\Local\ms-playwright` mit Exit 0.
  - [ ] Scope: `tests/Test-JobAgentUiBrowserAudit.ps1`, die dort verwendete Playwright-CLI-Aufloesung und bei Bedarf zugehoerige lokale Testhelper, `.gitignore`, `docs/test-matrix.json` und die CI-003-spezifische Funktionstestdatei unter `tests/`; keine Browser-Assertions, Produkt-HTML, Produktionsdaten, globale Playwright-Konfiguration, Benutzerprofilverzeichnisse oder Immutable-Pins ändern.
  - [ ] Ist-Stand (2026-09-16 16:09): `./ci.cmd supertest` endete mit Exit 1 und 24 `passed`, 1 `failed`, 4 `not_run`; `UI-001` brach im Playwright-CLI-Daemonstart mit `EPERM` beim Öffnen der Benutzerprofil-Datei `...\\ms-playwright\\daemon\\9bea5c76237be5c7\\jobagent-ui001-781b3d6f83ef4aa9afbfe71276e1adf2.err` ab.
  - [ ] Abhaengigkeiten: CI-002 muss die projektlokale npm-Toolauflösung beibehalten; ein dokumentierter lokaler Playwright-CLI-/Browsercache ist vor dem UI-Funktionstest erforderlich. Aufwand/Dauer: 0,5-1,5 PT / 4-12 Stunden bei lokal verfügbaren Browserartefakten. Prioritaetsscore: 98/100. Ordnungsbegruendung: Der Fehler blockiert vier Schlussfälle und damit die finale Abnahme von CI-002.
  - [ ] Risiken und Unsicherheiten: Die genaue Playwright-CLI-Konfigurationsvariable für den Daemonpfad und die lokale Browserartefaktverfügbarkeit sind noch nicht belegt; Downloads, globale Cachebereinigungen, Benutzerprofilberechtigungen oder das Deaktivieren des Browseraudits sind keine zulässigen Reparaturen.
  - [ ] Schritte:
    1. [ ] CI-003.1 - Startumgebung reproduzierbar inventarisieren: Den isolierten UI-Browsertest mit vollständig protokollierten, aber redigierten Playwright-, Temp- und Cacheumgebungsvariablen starten; CLI-Version, tatsächliche Daemon-/Browserpfade, Exitcode und Fehlerklasse in einem ID-basierten Evidence-JSON erfassen, ohne Benutzerprofile zu mutieren.
    2. [ ] CI-003.2 - Lokale Prozessumgebung kapseln: Eine testbare Helper-Funktion einführen, die für jeden UI-Lauf eindeutige Unterpfade von `.ci\cache\playwright` setzt, die gestartete CLI auf diese Umgebung begrenzt, alle temporären Unterpfade im `finally` entfernt und ursprüngliche Prozessumgebungsvariablen vollständig wiederherstellt.
    3. [ ] CI-003.3 - Fehler- und Erfolgspfade absichern: Eine neue CI-003-Testdatei prüft projektlokalen Daemonpfad, Umgebungsrestauration, fehlenden lokalen Browser/CLI fail-closed, nicht beschreibbaren lokalen Zielpfad und Cleanup; die Browseraudit-Fixture prüft danach weiterhin 390/800/1366/1920 px, sichtbaren Fokus, Overflow und Request-Invarianz unverändert.
  - [ ] Evidence: `docs/reviews/CI-003-acceptance.md`, `logs/jobagent/CI-003/<lauf-id>/environment.json` mit redigierten lokalen Pfaden, CLI-/Browserversionen, Exitcodes, bereinigten Childfehlern und Hashes sowie ein finaler Supertest-`summary.json`; keine Benutzerprofilpfade, Tokens oder Browserprofile versionieren.
  - [ ] Funktionstest: Neuer CI-003-spezifischer Test, `pwsh -NoProfile -File .\tests\Test-JobAgentUiBrowserAudit.ps1`, `pwsh -NoProfile -File .\tests\Test-JobAgentHtmlViewportAudit.ps1`, `pwsh -NoProfile -File .\tests\Test-JobAgentTestMatrix.ps1` und `pwsh -NoProfile -File .\tests\Test-JobAgentSupertestContract.ps1` jeweils Exit 0.
  - [ ] Audit: Vier Browserbreiten 390/800/1366/1920 px mit unveränderten Layoutakzeptanzkriterien; zusätzlich statischer Review, dass kein gestarteter Childprozess oder Artefaktpfad auf `C:\Users\ralph\AppData\Local\ms-playwright` oder einen anderen Benutzerprofilpfad verweist. Android-/Device-Lane `not-applicable`, da der Audit eine lokale Web-Berichtslane bleibt.
  - [ ] Supertest: Erst nach allen fünf grünen Funktionstests `./ci.cmd supertest`; Akzeptanz nur bei 30/30 `passed`, 0 `failed`, 0 `blocked`, 0 `not_run` und Exit 0. Ein erneuter Infrastrukturfehler erhält einen separaten Roadmappunkt und wird nicht durch Retry als bestanden gewertet.
  - [ ] Meilenstein/Parallelisierung: M4-Testinfrastruktur; CI-003.1 kann parallel zur Dokumentation von CI-002 laufen, CI-003.2 und CI-003.3 sind sequenziell, Browseraudit und Vollsupertest sind exklusiv.

- [ ] CI-002 Deterministische AJV-Schemapruefung ohne Zugriff auf den benutzerspezifischen npm-Cache #comment: Der Supertest wurde am 2026-09-16 um 15:55 CEST nach 1/28 bestandenen Faellen durch einen EPERM-Fehler beim Oeffnen von `C:\Users\ralph\AppData\Local\npm-cache\_cacache\tmp\8d147fda` abgebrochen.
  - [ ] Beschreibung: `tests/Test-JobAgentSchema.ps1` kompiliert und validiert das JSON-Schema mit AJV unter einer ausschliesslich projektnahen, pro Lauf eindeutig isolierten Cache-/Temp-Umgebung; der vollstaendige Supertest erreicht danach 28/28 Tests mit Exit 0, ohne `C:\Users\ralph\AppData\Local\npm-cache` zu lesen oder zu schreiben.
  - [ ] Scope: `tests/Test-JobAgentSchema.ps1`, eine neue zugehoerige Funktionstestdatei unter `tests/`, bei Bedarf `docs/test-matrix.json` und `tests/JobAgent.Supertest.psm1`; keine Aenderung an Schemafachregeln, Produktionsdaten, globaler npm-Konfiguration, fremden Benutzerverzeichnissen oder Immutable-Pins.
  - [ ] Ist-Stand (2026-09-16 15:55): `./ci.cmd supertest` endete mit Exit 1; `QA-006-CI-CONTRACT` bestand, `JA-002` scheiterte beim dynamischen Nachladen von `ajv-formats` ueber `npx --yes` mit `EPERM`, 26 nachfolgende Pflichtfaelle blieben `not-run`.
  - [ ] Abhaengigkeiten: Ein belegter lokaler AJV-/ajv-formats-Artefaktbestand oder eine explizit dokumentierte, erlaubte Paketbereitstellung ist Voraussetzung; die Aenderung muss vor einem neuen Vollsupertest und vor Abschluss von `TD-0056` erfolgen. Aufwand/Dauer: 0,5-1 PT / 4-8 Stunden bei einer verfügbaren lokalen Paketquelle. Prioritaetsscore: 96/100. Ordnungsbegruendung: Der Fehler blockiert die gesamte Funktionsmatrix bereits im zweiten Pflichtfall.
  - [ ] Risiken und Unsicherheiten: Der konkrete Sperrhalter des globalen npm-Caches und die Verfuegbarkeit lokaler AJV-Artefakte sind nicht ermittelt; ein Netzwerkdownload, eine globale Cachebereinigung oder das Lockern von Benutzerberechtigungen sind nicht autorisiert und duerfen nicht als Reparatur verwendet werden.
  - [ ] Schritte:
    1. [ ] CI-002.1 - Reproduzierbarkeit und Artefaktinventar: Den fehlerhaften `npx`-Aufruf aus `tests/Test-JobAgentSchema.ps1` mit einem neuen isolierten `NPM_CONFIG_CACHE` und `TMP`/`TEMP` unter einer eindeutigen Temp-Wurzel ausfuehren; Exitcode, effektiv aufgeloeste Paketversionen, verwendete Pfade und fehlende Artefakte in einem ID-basierten Evidence-JSON erfassen, ohne den globalen Cache zu mutieren.
    2. [ ] CI-002.2 - Deterministische Testausfuehrung implementieren: Eine klar abgegrenzte Helper-Funktion fuer die lokale AJV-CLI-Aufloesung und den isolierten Cache in `tests/Test-JobAgentSchema.ps1` einfuehren; sie muss fehlende oder ungueltige lokale Artefakte mit einer stabilen, redigierten Fehlermeldung fail-closed melden und Temp-Verzeichnisse im `finally` entfernen.
    3. [ ] CI-002.3 - Funktionstest und Matrixvertrag erweitern: Eine neue Testdatei prueft Erfolg mit isoliertem Cache, fehlendes AJV-Artefakt, nicht beschreibbaren Cachepfad, Redigierung von Benutzerpfaden sowie Temp-Cleanup; die Matrix nimmt den Test genau einmal in korrekter Abhaengigkeitsreihenfolge auf und der Runner weist Doppelung, Selbstaufnahme oder eine nicht vorhandene Testdatei weiterhin ab.
  - [ ] Evidence: `docs/reviews/CI-002-acceptance.md`, ein `logs/jobagent/CI-002/<lauf-id>/`-Bericht mit isolierten Cachepfaden, Paketversions-/Hashinventar, Child-Exitcodes und bereinigtem Fehlertext sowie ein finaler Supertest-`summary.json`; keine Benutzerpfade, Tokens oder globale Cacheinhalte im versionierten Artefakt.
  - [ ] Funktionstest: `pwsh -NoProfile -File .\tests\Test-JobAgentSchema.ps1`, der neue CI-002-spezifische Test und `pwsh -NoProfile -File .\tests\Test-JobAgentTestMatrix.ps1` jeweils mit Exit 0; danach `pwsh -NoProfile -File .\tests\Test-JobAgentSupertestContract.ps1` mit Exit 0.
  - [ ] Audit: Statischer Review der geaenderten Test- und Runnerpfade auf keine globalen Cache-, Netzwerk- oder Benutzerprofilmutationen; keine UI-Aenderung, deshalb Browser-/Viewport-/Device-Pruefung `not-applicable` mit Begründung im Evidence-Bericht.
  - [ ] Supertest: Erst nach den vier gruenen Funktionstests `./ci.cmd supertest`; Akzeptanz nur bei 28/28 `passed`, 0 `failed`, 0 `blocked`, 0 `not_run` und Exit 0. Bei erneutem Fehler ausschliesslich den betroffenen CI-002-Funktionstest analysieren und einen neuen Roadmappunkt anlegen.
  - [ ] Meilenstein/Parallelisierung: M4-Testinfrastruktur; CI-002.1 kann parallel zur read-only Analyse von `TD-0056` laufen, CI-002.2 und CI-002.3 sind sequenziell, der Vollsupertest ist exklusiv.

Die abgeschlossenen Punkte QA-001 bis QA-006 sind vollständig in `Roadmap_archive.md` archiviert. Die zugehörigen Abnahmen bleiben unter `docs/reviews/` referenziert. `TD-0056` (CI-Drift) bleibt ein separater technischer Befund und wird durch diesen Punkt nicht als erledigt markiert.
